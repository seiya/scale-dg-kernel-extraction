# TODO

作成 2026-09-02、`236a74a` 時点。出所はすべて `reports/` の節番号で示す。
根拠が無い項目は「レポートは沈黙」と明記する。推測で埋めない。

規約は `AGENTS.md`。とくに次の 3 つが本ファイルの項目選定を決めている。

- 「できることはやる」——「残る利得が小さい」は止める理由にならない。
  止める理由は「測れるものが無くなった」だけ。
- 「未説明の測定はモデルの欠陥」——性能が動かなくても機構の特定は成果物。
- 「否定的結果も結果」——不採用は数値と機構つきで記録する。

---

## 1. 測定不要（記録の更新だけで閉じる）

`FUSED_TC` の最適化が進んだのに `reports/README.md` の表が追随していない。
論文の主比がここから計算されるので、優先度は高い。

- [ ] **`README.md` の `FUSED_TC` 行を 4 次数で更新する。**
  - p=31: 359.7 → **354.7**（`p31_gap_study.md` §25.2、PDL 採用後、job `75806`）
  - p=63: 421.7 → **409.2**（`p63_gap_study.md` §50.6）。**README に一度も入っていない**（§50 への言及自体が無い）
  - p=127: 621.5 → **590.8**（`p127_gap_study.md` §23.8）。最速経路横断表の下に追記はあるが本文表は旧値
  - p=255: 918.9 → **883.8**（`p255_gap_study.md` §22）。README 冒頭の索引には 883.8 とあるのに本文表は旧値
- [ ] **主比 B（`FUSED_TC` / `FUSED`）を現行値で引き直す。** 4 次数が古い分子で
  計算されている。現行値: p=63 **1.32×**（1.47× から）、p=127 **1.51×**（1.85× から）、
  p=255 **2.11×**（2.04× から）、p=31 は §25.2 が 1.42× と記載済み。
  **この 3 つはどのレポートにも書かれていない。**
- [ ] **p=127 の主比 B の不一致を解消する。** README p=127 節 1.85×（分子 621.5）と
  `p127_gap_study.md` §17.1 の 1.61×（分子 706.1）。分母は同じ 1149.9 で分子が別時点。
- [x] ~~`reports/README.md` 「経路の役割」節を `AGENTS.md` に揃える。~~
  2026-09-02 完了。節を「何が固定で、何が測定か」に書き直し、経路ごとの
  固定と自由を表にした。`FUSED` が全力最適化対象であること、TC 実装を
  取り込まない理由（主比 A が消える）、`GEMM` の周辺は共有であって凍結では
  ないこと、閾値は規約に書かないこと、役割外アブレーションの扱いを明記。
- [ ] **p=7 `FUSED_DFMA` の 2 値を整理する。** 427.8（`tc_paper_survey_2407.09621.md` §15、
  README 表）と 424.1（同 §17）。どちらも 2026-08-29 の login 3-run。
- [ ] **p=15 `FUSED_TC` の 271.8（README）対 272.8（§20.1）** は §20.1 に明記済みの既知差だが、
  表側にも注記があるとよい。
- [ ] **`p63_gap_study.md` §52 は `Step loop per stage` で測っている**（base 489.6）。
  README の device 421.7 / 409.2 とは物差しが違うので、同一表に並べない旨を明記する。

## 2. 測定が要るもの（賭け金順）

### 2.1 p=31 `FUSED_TC`: ワープ mma タイル拡大 + チャンクループ二重バッファ

- [ ] **未試行。符号則が最大の勝ちを予測する側にいる。**
  `p63_gap_study.md` §52.3 が符号則を明文化している ——**効くかどうかはチャンク数で決まる**:
  p=255 は 16 個で **−27.7%**（`p255_gap_study.md` §4.1）、p=127 は 2 個で **−4.25%**
  （`p127_gap_study.md` §23.3）、p=63 は 1 個で **+2.8〜3.1% で棄却**（§52.3）。
  **p=31 は 16 チャンク**（`p31_gap_study.md` §18.3 が「ループが 16 回回る」と記載）。
  §18.4 と §19.2 の 2 回の「探索終了」宣言は、どちらもこの軸を含んでいない。
  §18.3 が測ったのはチャンクループ末尾バリアの前送り（+0.58%）だけ。
- [ ] 併せて `p127_gap_study.md` §23.2 / §23.9 の解法（**占有率を下げてレジスタ予算を作る**）を
  当てる。p=31 xz は §19.2 / §27.1 で「128 レジスタ・512 スレッド・占有率 25% は動かせない」
  と閉じているが、§23.9 の「塞がっていたのは占有率であって性能ではなかった」は
  まさにその前提を逆向きに開ける。
- 効果幅の実績: p=255 −27.7%、p=127 −4.25%。単独では効かず**組でのみ効く**
  （p=255 §4.1、p=127 §23.3 とも非加法性を記録）。

### 2.2 p=63 / p=127 `FUSED`（CC）: 出力タイル × レジスタ予算の横展開

- [ ] **どちらも未試行。レポートは沈黙。**
  出所は `p255_gap_study.md` §15.49–§15.92（1 スレッド複数出力でオペランド共有 +
  レジスタ予算を先に決める）。p=31 で **−29.9%**（`p31_gap_study.md` §27.3）、
  p=15 で部分採用 **−1.72%**（`p15_gap_study.md` §27.2）。
- 判定基準は `p15_gap_study.md` §27.3 が明文化している ——「**(A) が効くのは
  shared wavefront で詰まっている次数だけで、global で詰まっている次数には移らない**」。
  p=63 CC（§25.2: L1/TEX 97.3%、shared wf 87.9M、mio 7.64）も
  p=127 CC（§17.5: L1/TEX 92%、shared wf 176.3M、mio 5.65）も**勝ち側**。
- 両者の終了宣言（`p63_gap_study.md` §25.5、`p127_gap_study.md` §17.5）は 2026-08-30 で、
  この軸が生まれる前。分母が大きい（618.6 / 1149.9）ので主比 B への影響も大きい。
- [ ] p=7 CC にも当てる。ただし `p7_gap_study.md` §0 / §2 の律速は L1/TEX の
  global ロード待ち（long scoreboard 45%）で、上の判定基準では**負け側**（p=15 と同型）。
  優先度は低いが、判定基準の 3 点目のデータにはなる。

### 2.3 `FUSED_DFMA` を現行ソースで測り直す

- [ ] **機構比 A（`FUSED_TC` / `FUSED_DFMA`）が現時点で iso-schedule になっていない。**
  ソースは乖離していない（`p127_gap_study.md` §23.7、`p63_gap_study.md` §50.5、
  `p31_gap_study.md` §25.4 がビット一致を確認）が、**公表値が古いバイナリ由来**。
  README p=127 節が「`FUSED_DFMA` の 2137.3 µs は §15 の測定で、§18–19 の y / 面変更は
  入れていない」と自認している。同じ問題が p=63（§18 / §50 以降）、p=31（§25 以降）、
  p=255（§17–22 以降）にもある。
- `AGENTS.md` の `CUDAFORTRAN_FUSED_DFMA` の定義（「MMA 命令だけの iso-schedule
  アブレーション」）に直接関わるので、性能課題ではなく**契約の課題**として扱う。
- 2.1 / 2.2 と同じジョブで取れる。

## 3. 「未着手 / 未実装」と明記されているもの

| 経路・次数 | 項目 | 出所 | 天井 |
|---|---|---|---|
| [ ] p=15 `FUSED_TC` | 面点 M 側を global 再取得ではなく **shared 経由で再配布**（`VMapM` は使い続ける） | `p15_gap_study.md` §14.6「**未実装・未測定である**」 | **−7.6%**（26 µs、349 → 323） |
| [ ] p=31 `FUSED_TC` | xz ld 4.32% / y st 16.34% のバンクコンフリクト | `p31_gap_study.md` §14.6「**不採用ではなく未着手として残す**」 | 未評価 |
| [ ] p=255 `FUSED_TC` | `BK=32`（バリア半減） | `p255_gap_study.md` §7「**未実装**」 | — |
| [ ] p=255 `FUSED_TC` | **x と y の融合**（同じ出力タイルなので `dqdt` の RMW が 1 回減る） | 同 §7・§9「**未実装**」 | — |
| [ ] p=255 `FUSED` | z の D の global 化 | 同 §15「z は y と同型のため**未実装**」 | — |
| [ ] p=7 `FUSED_TC` | `m16n8k*` FP64 mma | `tc_paper_survey_2407.09621.md` §7.5「**未着手**」（§2 B-1） | — |

`BK=32` と x/y 融合はどちらも**レジスタ予算が理由**で止まっている
（プリフェッチ 48→96 で `MINB=3` に入らない / 蓄積器 2 組で 224 超）。
2.1 の「占有率を下げて予算を作る」が効けば、この 2 つも再検討の対象になる。

## 4. 未説明の測定（`AGENTS.md` の基準ではモデルの欠陥）

- [ ] **p=7 `FUSED_TC` の shared store コンフリクトが 2.41M → 4.10M に増える理由。**
  `tc_paper_survey_2407.09621.md` §13.5 が「未解明のまま残る」と明記。
  時間への寄与も未評価。
- [ ] **p=31 `FUSED_TC` の面 2,4 が 5 形すべて負ける理由。** 天井は −17.8%（§18.2）
  なのに、実装 5 形が +25.6% / +25.6% / ±0 / +31.5%（§18.3・§19.1）/ +2.48%（§26）。
  §26 は「消した `VMapM` の coalesced ロードが、占有率 25% のこのカーネルでは
  レイテンシ隠蔽の原資だった」と部分的に説明しているが、5 形全部は説明していない。

## 5. レポートが沈黙している判断

いずれも「やらない」と決めた記録が無い。決めたなら記録し、決めていないなら測る。

- [ ] **p≥511（511 / 575 / 767 / 1023）に融合経路が存在しない理由。**
  4 本の gap study を grep しても `FUSED_TC` / CC 融合への言及がゼロ。
  README 表注は「p≥511 は `GEMM` / `GEMM_FUSED` のみ」と事実を書くだけで、
  **書かない判断の検討記録が無い**。
- [ ] **p=7 / p=15 への PDL 適用可否。** PDL は `p127_gap_study.md` §19 →
  `p63_gap_study.md` §50 → `p31_gap_study.md` §25 と横展開されたが、
  p=7 / p=15 では**一度も言及がない**。
- [ ] **p=7 `FUSED_TC` にチャンクループがあるかどうか。**
  `tc_paper_survey_2407.09621.md` に記述が無いので 2.1 の符号則を当てられない。

## 6. 古い可能性のある「探索終了」宣言

**覆った 5 例はいずれも同じ理由だった** ——「当時の候補集合に、後で他次数から来る軸が
入っていなかった」。したがって残る宣言も同じ理由で古い可能性がある。

覆った実績:

| 覆された | 覆した | 内容 |
|---|---|---|
| `p63_gap_study.md` §20 | §50.6 | 「§20 の『探索終了』は**当時の候補が尽きたという意味**」。PDL −3.0% |
| `p127_gap_study.md` §11.13 / §20.3 | §23.9 | 「**塞がっていたのは占有率であって性能ではなかった**」 |
| `p15_gap_study.md` §22.3 | §27 | 「(A)/(B) の軸を測っていなかったという意味で不完全だった」 |
| `p31_gap_study.md` §20.4 | §27.6 | 「**取り消す**。天井を測っていなかったのは (A) の側」 |
| `p255_gap_study.md` §16 | §17 | 「当時はここで探索終了と書いたが、§17 で残っていた」 |

再点検の対象:

- [ ] `p7_gap_study.md` §4（`FUSED`）
- [ ] `p63_gap_study.md` §25.5（`FUSED`）
- [ ] `p127_gap_study.md` §17.5（`FUSED`）
- [ ] `p255_gap_study.md` §22（`FUSED_TC`）
- [ ] `p255_gap_study.md` に `FUSED` の探索終了宣言が**無い**（§23.5 が「本番コードは
  §15.92 のまま」で終わる）。天井は §15.87 / §15.90 と INNER=1 の −36.5%。
- [ ] p=7 `FUSED_TC` にも探索終了宣言が無い（`tc_paper_survey` §6/§7/§13/§15/§16 に分散）。

## 7. GEMM 系列で棚卸ししていない部分

`reports/gemm_assignment_and_carrier.md` §6 は閉じたが、それは**この一連の作業が
自分で開けた穴**であって、GEMM 系列全体の棚卸しではない。

- [ ] **`GEMM` パスの周辺カーネル融合。** `volume_flux_kernel` と `elembnd_flux_kernel` を
  1 本にまとめる案。禁じられておらず、`elembnd_flux_group_kernel` のような別分割も
  既に存在するのに、測っていない。GEMM 系 5 パス全部に同時に入れる必要がある。
- [ ] **`GEMM` パスの 3 本並列。** 3 本の volume GEMM は独立なので別ストリームで
  重ねられる。`GEMM_FUSED` には `overlap_y` 相当があるが `GEMM` には無く、測っていない。
  低次数で効く可能性（p=15 `GEMM_FUSED` で x‖y を外すと +1.5%）、
  高次数は赤字（`p575_gap_study.md` §11.1 / §11.7）。
- [ ] **p≥511 に今回の 2 つの知見を当てる。** (a) 汎用 64×128 の N 述語化
  （`gemm_assignment_and_carrier.md` §1）、(b) `GEMM_CUTE` の x に repad が
  入っていなかった食い違い（同 §3。p=127/256 では −0.25% / −0.23%）。
  **(b) は経路間の差なので、あれば直すべきもの。**
- [ ] **Ozaki 系列。** 今回まったく触れていない。
- [ ] **p≥511 の GEMM 系「探索終了」宣言の再点検。** `p511_gap_study.md` §11–§13、
  `p575_gap_study.md` §11、`p767_gap_study.md` §11–§15、`p1023_gap_study.md` §12–§13。
  いずれも今回の変更前のツリーで測られている。`p31_gap_study.md` §10 と
  `p15_gap_study.md` §26.1 が機構ごと反証されたのと同じ形で古い可能性がある。

## 8. 完了（参照用）

- `reports/gemm_assignment_and_carrier.md` §6 の全項目。担い手 3 候補 × 全次数、
  volume GEMM ごとのライブラリ割り当て、epilogue パッケージ 3 成分、
  および派生した未解決 6 件。
- `GEMM_FUSED` の到達点: p=7 1747.1 → 1482.0、p=15 658.0 → 597.9、
  p=31 708.0 → 545.4、p=63 571.1 → 564.3。
