# TODO

作成 2026-09-02、`236a74a` 時点。出所はすべて `reports/` の節番号で示す。
根拠が無い項目は「レポートは沈黙」と明記する。推測で埋めない。

規約は `AGENTS.md`。とくに次の 3 つが本ファイルの項目選定を決めている。

- 「できることはやる」——「残る利得が小さい」は止める理由にならない。
  止める理由は「測れるものが無くなった」だけ。
- 「未説明の測定はモデルの欠陥」——性能が動かなくても機構の特定は成果物。
- 「否定的結果も結果」——不採用は数値と機構つきで記録する。

---

## 1. 測定不要（記録の更新だけで閉じる）

`FUSED_TC` の最適化が進んだのに `reports/README.md` の表が追随していない。
論文の主比がここから計算されるので、優先度は高い。

- [x] ~~**`README.md` の `FUSED_TC` 行を 4 次数で更新する。**~~
  2026-09-02 完了。測定表は書き換えず、次数別節（p=31 / 63 / 127 / 255）に
  「追記（2026-09-02）」を足して現行値・出所ジョブ・FLOP 比の引き直しを書き、
  「最速経路だけ横断」の下に **6 次数の一覧表**（上表の値／現行値／出所／
  現行 `FUSED` 分母／主比 B）を追加した。裏取り済みの現行値:
  p=31 **354.7**（`p31_gap_study.md` §25.2、job `75804`/`75806`）、
  p=63 **409.2**（`p63_gap_study.md` §50.6、job `75806`）、
  p=127 **590.8**（`p127_gap_study.md` §23.8、job `75895`）、
  p=255 **883.8**（`p255_gap_study.md` §22 の 883.781、job `73986`）。
- [x] ~~**主比 B（`FUSED_TC` / `FUSED`）を現行値で引き直す。**~~
  2026-09-02 完了。**各 gap study の現行 `FUSED` を分母に検算したところ、
  本 TODO が挙げていた p=63 1.32× と p=127 1.51× は誤りだった**ので、
  実際に計算した値を書いた。分母は 4 次数とも上表と同じ（`FUSED` 側は未変更）:
  p=31 503.4 / 354.7 = **1.42×**（§25.2 の記載と一致）、
  p=63 618.6 / 409.2 = **1.51×**（1.47× から。TODO の 1.32× は不成立）、
  p=127 1149.9 / 590.8 = **1.95×**（1.85× から。TODO の 1.51× は p=63 の値と重なる）、
  p=255 1868.6 / 883.8 = **2.11×**（2.04× から。TODO どおり）。
  p=63 の 1.51× と p=127 の 1.95× はここが初出。
- [x] ~~**p=127 の主比 B の不一致を解消する。**~~
  2026-09-02 完了。README p=127 節に「分母はどちらも §17 の CC 1149.9（job `70527`）で
  共通、分子だけが別時点」と明記し、時系列を
  **1.61×（§15 の TC 706.1、§18–20 前）→ 1.85×（§19 の TC 621.5）→
  1.95×（§23.8 の TC 590.8、現行）**として並べた。§17.1 の 1.61× は当時の測定として残す。
- [x] ~~`reports/README.md` 「経路の役割」節を `AGENTS.md` に揃える。~~
  2026-09-02 完了。節を「何が固定で、何が測定か」に書き直し、経路ごとの
  固定と自由を表にした。`FUSED` が全力最適化対象であること、TC 実装を
  取り込まない理由（主比 A が消える）、`GEMM` の周辺は共有であって凍結では
  ないこと、閾値は規約に書かないこと、役割外アブレーションの扱いを明記。
- [x] ~~**p=7 `FUSED_DFMA` の 2 値を整理する。**~~
  2026-09-02 完了。**表の値は §15 の 427.8 を採る**と README p=7 節および
  `tc_paper_survey_2407.09621.md` §15・§17 の双方に注記した。根拠は
  「README p=7 表の他の行（`OPENACC_*` / `SPLIT` / `FUSED_TC` / `GEMM`）が
  §15 の同一セットの 3-run 中央値なので、行間の比較を別測定の混合にしない」。
  差は 0.87% で login 3-run のばらつきの範囲、メカニズム比は 1.56× 対 1.57× で
  結論は動かない。§17 の 424.1 は同じ量の別測定として残した。
- [x] ~~**p=15 `FUSED_TC` の 271.8（README）対 272.8（§20.1）**~~
  2026-09-02 完了。README p=15 節に既知差（0.4%、§20.1 の device 3-run
  15.542/15.562/15.542 ms 中央値）として注記し、表の値は経路横断セットの
  271.8 のままとした。主比 B を 272.8 で引くと 1.22× で 1.23× と結論は同じ。
- [x] ~~**`p63_gap_study.md` §52 は `Step loop per stage` で測っている**（base 489.6）。~~
  2026-09-02 完了。`p63_gap_study.md` に **§52.5** を追加し、489.6 が
  `Step loop per stage`（halo + RK 更新込みの 1 ステップ ÷3）であること、
  §50.6 の 409.2 と README 表の 421.7 は `CUDA device fused tendency`、
  §18 の 477.2 は `Cal_tend` という**3 種類の別物**であること、
  同一表に並べてはならないこと、§52 の判定は同一物差し上の相対比較なので
  有効であることを明記した。README の p=63 節にも同じ断りを入れた。
  併せて、§52 の見出しが入力を `Ne=2³` / `nstep=100` と書いており
  §51・README の `Ne=4³` / `nstep=20` と食い違う点も記録した（本作業では確定不能）。

## 2. 測定が要るもの（賭け金順）

### 2.1 p=31 `FUSED_TC`: ワープ mma タイル拡大 + チャンクループ二重バッファ

- [x] ~~**未試行。符号則が最大の勝ちを予測する側にいる。**~~
  2026-09-02 完了、**採用ゼロ**（`p31_gap_study.md` §30、job `77439` / `77447` /
  ncu `77440`）。**本項目の前提が誤っていた**: 符号則の「チャンク」は縮約方向の
  パネル分割（`BKD127` / `BK255` / `BK63`）であって、`p31_gap_study.md` §18.3 が
  「16 回回る」と書いた `jl` ループは **j 平面のバッチループ**である。Nq=32 の
  縮約 32 個は 1 枚の shared 平面に丸ごと載っているので、**p=31 の縮約チャンク数は
  1**——符号則は p=63 と同じ「棄却」を予測していた。実測もそのとおり:
  単独 (a) **+1.11%**、単独 (b) **+1.04%**、**組 +12.05%**（いずれも占有 GPU、
  12 ラウンド交互、レンジ非重複）。非加法性は p=31 にも**あるが符号が逆**で、
  組は単独の和よりはるかに悪い。機構は ncu で確定: (b) はバリアを 33 → 18 本に
  減らして barrier stall 1.09 → 0.865 を得るが**命令が +12.1%**（バッファ添字が
  実行時値になり LDS が即値オフセットに畳めない）、(a) は shared ld ウェーブフロント
  **−51.1%**・命令 −16.2%・`lg_throttle` 6.26 → 1.62 を得るが、**global セクタが
  動かないまま DRAM read が +12.0%**（128 スレッド CTA が 4 倍長く走り、
  2 スラブ間の面 1,3 再利用が L2 の寿命から外れる）で、占有率も 25% → 11.9%。
- [x] ~~併せて `p127_gap_study.md` §23.2 / §23.9 の解法（**占有率を下げてレジスタ
  予算を作る**）を当てる。~~
  2026-09-02 完了（`p31_gap_study.md` §30.5）。**§19.2 / §27.1 の結論は今も
  正しいが、書かれていた理由は誤っていた。** 「128 レジスタ天井」は 512 スレッドと
  いう構成の性質で、128 スレッド（2×2 タイル）にすれば ptxas は **255 レジスタを
  スピル 0** で使う。占有率も 24.99% → 11.86% に下げられる。**それでも実時間は
  +1.11%**。動かない本当の理由は、p=31 xz が shared でも命令でもなく
  **global リクエスト（`lg_throttle`）と DRAM** で詰まっていること。
  §23.9 の「塞がっていたのは占有率であって性能ではなかった」は p=127 では
  成り立ち、**p=31 では成り立たない**（p=127 xz は `long_scoreboard` 11.37 の
  global 待ちで、隠せる相手だった）。
- 副産物 2 件（同 §30.7 / §30.8）: **最初の平面 global ロードを面バリアの手前に
  出すと +1.40%**（§18.3 の `Escale` 前送り +2.3% と同型の 3 例目）、
  **静的 shared を動的 shared にするだけで +1.70%**（ただし 2×2 タイルでは
  符号が反転して動的の方が 1.5% 速い）。`ncu` クロック固定バイアスの 7 例目
  （(a) は ncu duration −1.9% に対し実時間 +1.11%）。
- ノブは `fused_kernel_geom.h` に `P31_XZ_TM/TN/DB/DYN/EARLY/MINB` と
  `P31_Y_*` として残した（既定は改修前の形。`P63_XZ_DB` と同じ扱い）。

### 2.2 p=63 / p=127 `FUSED`（CC）: 出力タイル × レジスタ予算の横展開（2026-09-02 完了、−25.0% / −28.4%、p=7 も −3.4%）

- [x] ~~**どちらも未試行。レポートは沈黙。**~~
  2026-09-02 完了、**両方とも採用**（job `77601` / `77616` / `78001`、ncu `77603` /
  `77990` / `78002`）。判定基準（`p15_gap_study.md` §27.3）は **p=63 でも p=127 でも
  成立した**。
  - **p=63 `FUSED`: 621.7 → 466.1 µs/stage（−25.0%）**
    （[`p63_gap_study.md`](reports/p63_gap_study.md) §53–§54）。
    xz / y とも 1 スレッド 2 i（`TI=2`）× 256 スレッド × 128 レジスタ。
    shared wavefront −38%、命令 −18%、`mio_throttle` 7.6 → 1.4、
    **global セクタも DRAM バイトも動かない**。占有率は 48% → 24% に落ちるのに速い。
    主比 B は 1.51× → **1.14×**。CC 融合がこの次数で初めて `GEMM_FUSED`
    （564.3）を 17.4% 下回った。
  - **p=127 `FUSED`: 1150.7 → 823.6 µs/stage（−28.4%）**
    （[`p127_gap_study.md`](reports/p127_gap_study.md) §24–§25）。
    xz は `TI=2` × 256 スレッド × `BK=16`、y は `TI=2` × 128 スレッド × 16 j。
    shared wavefront xz −38% / y −48%、`mio_throttle` 5.65 → 0.24 で
    **L1/TEX の屋根から降りた**。主比 B は 1.95× → **1.39×**。
  - **(B) の符号則は両次数で再現した**: 同じ (A) の操作がレジスタ予算の内側では
    −12〜−16%、外側（スピル側）では +4%〜+346% に反転する（p=63 の
    `BK=32`/`minBlocks=4` は 440 B スピルで +346%）。
  - **副産物（defect の発見と修正）**: `TI=2` にするとエピローグの
    `Escale` / `flux_bnd` が **8 B ロード 2 本・レーンストライド 16 B**として
    発行され、どちらも warp の 512 B フットプリント全体を触るので global セクタが
    2 倍になる。`double2` にまとめると p=63 −4.9% / p=127 −3.0%
    （p=127 は最良の `BK`/`minBlocks` まで動く）。p=15 / p=31 / p=255 の CC は
    最初から `double2` なので同じ defect は無い。
  - **`BK` チャンクは p=63 では負ける（+11.7%）**。1 枚の 64×64 平面を x が i 軸、
    z が k 軸で使うので、`BK` を切っても shared が減るだけでブロック数は
    レジスタ側が決めたまま動かない。p=127 は 4 枚のパネルを持つので勝つ。
  - これにより §6 の「再点検の対象」のうち `p63_gap_study.md` §25.5 と
    `p127_gap_study.md` §17.5 の 2 つが閉じた（覆った探索終了宣言は 5 例 → 7 例）。
    `reports/README.md` の p=63 / p=127 節、6 次数一覧表、索引行も同じ変更で更新した。
- [x] ~~p=7 CC にも当てる。~~
  2026-09-02 完了、**採用 −3.44%**（[`p7_gap_study.md`](reports/p7_gap_study.md) §5、
  job `78030` / ncu `78031`）。302.7 → **292.3 µs/stage**、主比 B 1.10× → **1.06×**。
  p=7 CC は §2 の時点で既に 1 スレッド 2 出力（`node1 = tid`、`node2 = tid + 256`）
  だったので、512 ノードを 128 スレッドで覆って **4 出力**（96 レジスタ、
  5 ブロック/SM）にした。`fused_kernel_geom.h` の `P7_THREADS` は `FUSED_TC` と
  共有なので触らず、`cuda_dg_kernels_fused.cu` に `P7_CC_THREADS` /
  `P7_CC_BPSM` を足している。
- **判定基準（`p15_gap_study.md` §27.3）は 3 点目のデータで半分だけ覆った。**
  「(A) は global 律速の次数には移らない」は **shared wavefront については
  正しい**（p=7 −6.4%、p=15 −13〜22% に対し p=31 −35%、p=63 / p=127 −38%）が、
  **時間については正しくない**: 出力タイルは shared だけでなく**命令数と
  バリア**も出力で割る（p=7 は命令 −22.1%、バリア stall 5.00 → 0.89、
  global セクタとリクエストは 1 本も動かない）。判定基準は
  「**(A) の大きさは shared wavefront の詰まり方で決まる**」と読み替えるのが
  正しい。5 次数の実測は p=31 −29.9% / p=127 −28.4% / p=63 −25.0% /
  p=7 −3.4% / p=15 −1.7% で、shared wavefront の減り方と同じ順序に並ぶ。
- 副産物: `minBlocks` を **スピルの手前で**上げすぎても壊れる例が p=7 で出た
  （`minBlocks` 7・8 はスピル 0 のまま +44%。shared 31 KB では 7 枚しか入らず、
  要求した占有率が達成できないままレジスタだけ絞られる）。(B) は
  「スピルさせない」だけでなく「達成可能な占有率を要求する」でもある。
- これで §6 の「再点検の対象」のうち `p7_gap_study.md` §4 も閉じた。

### 2.3 `FUSED_DFMA` を現行ソースで測り直す（2026-09-02 完了）

- [x] ~~**機構比 A（`FUSED_TC` / `FUSED_DFMA`）が現時点で iso-schedule になっていない。**~~
  2026-09-02 完了（job `78053`、node `c384`、commit `fcf1872` + 下の defect 修正、
  6 次数 × 3 経路 × 12 ラウンドを**同一ジョブでインターリーブ**、
  物差しは `CUDA device fused tendency` ÷ (steps × 3)、3 経路ともレンジ非重複）。

  | p | `FUSED_TC` | `FUSED_DFMA` | **機構比 A** | 旧 A | 旧 DFMA | DFMA の差 |
  |---:|---:|---:|---:|---:|---:|---:|
  | 7 | 273.54 | 424.31 | **1.551×** | 1.556× | 427.8 | −0.82% |
  | 15 | 272.32 | 448.99 | **1.649×** | 1.643× | 446.6 | +0.53% |
  | 31 | 355.56 | 786.71 | **2.213×** | 2.228× | 790.1 | −0.43% |
  | 63 | 408.63 | 838.49 | **2.052×** | 2.068× | 846.2 | −0.91% |
  | 127 | 591.93 | 1455.77 | **2.459×** | 3.618× | 2137.3 | **−31.9%** |
  | 255 | 885.30 | 2017.00 | **2.278×** | 2.261× | 1998.0 | +0.95% |

  **動いたのは p=127 だけ。** §18–20 と §23 は mma に触っていないので DFMA にも
  同じだけ効いており、**旧 3.62× のうち 1.16 ポイントは mma ではなく日程の改良
  だった**。README p=127 節の「§18–19 を入れていない」という自認は解消した。
  残る 5 次数は ±1% 以内で結論は動かない（p=255 だけ +0.95% と遅い側だが、
  旧値が別ジョブなので断定していない。`p255_gap_study.md` §25）。
- [x] ~~ソースが内積以外で乖離していないことの確認~~
  `cuda_dg_kernels_tc.cu` 全体で `UseTc` に依存する分岐は `mma_m8n8k4_f64` の中の
  **1 か所だけ**（`grep 'if constexpr (UseTc)\|UseTc ?\|!UseTc'`）。起動形状・
  shared 量・バリア・エピローグ・PDL 属性・`4b95aca` の `P31_*` ノブ 55 か所は
  どれも `UseTc` を見ない。Fortran 側のラッパとディスパッチも対称。実測でも
  点変化係数の owned `dqdt(:,1:Ne)` が **6 次数すべてで TC / DFMA ビット一致**、
  `CUDAFORTRAN_SPLIT`（p=255 のみ `GEMM`）対照で max abs 1.78e-15〜3.55e-15。
- [x] ~~**副産物: `fcf1872` では `FUSED_TC` が p=63 / p=127 で起動しなかった。**~~
  `4b95aca` が p=63 / p=127 の PDL y カーネル起動の `ycfg.dynamicSmemBytes` を
  `smem_y` から `0` にしていて、`extern __shared__` を使うこの 2 カーネルが
  `CUDA_ERROR_ILLEGAL_ADDRESS` で落ちる。2 行戻した
  （`p63_gap_study.md` §55.1）。**最速経路が 2 次数で起動不能になっていたことに、
  古い値を公表し続けていたせいで気付けなかった** —— 本項目を「性能課題ではなく
  契約の課題」として立てた判断が、当初の想定より強く裏付けられた形である。
- 記録先: p=7 `tc_paper_survey_2407.09621.md` §18、p=15 `p15_gap_study.md` §29、
  p=31 `p31_gap_study.md` §31、p=63 `p63_gap_study.md` §55、
  p=127 `p127_gap_study.md` §26、p=255 `p255_gap_study.md` §25、
  横断表は `reports/README.md`「最速経路だけ横断」の追記（2026-09-02、3 回目）。

### 2.4 `FUSED_DFMA` のレジスタ予算 —— 測定・機構同定とも完了、**方針決定済み: 2 列**（2026-09-03）

出所は `tc_paper_survey_2407.09621.md` §19.9（p=7 の 1 例目）と
`p255_gap_study.md` §26.9「副産物 2（未評価）」。記録先は新規の
[`dfma_register_budget.md`](reports/dfma_register_budget.md)。

- [x] ~~**iso-schedule の代金を 6 次数で測る。**~~ 2026-09-03 完了
  （job `78408`、node `c190`、commit `3d4045c` + ノブ追加、12 ラウンド交互、
  掃引 20 版すべて次数内でレンジ非重複）。`__launch_bounds__` は `UseTc` を
  見ないので DFMA は TC 向けの予算を着せられており、**production で
  p=7 32 B / p=63 y 16 B / p=127 y 104 B + xz 112 B / p=255 56–80 B × 4 本
  を DFMA 側だけスピルする**（TC 側は 6 次数とも 0）。

  | p | `FUSED_TC` | DFMA iso | **A (iso)** | DFMA 単体最適 | **A (最適)** |
  |---:|---:|---:|---:|---:|---:|
  | 7 | 253.83 | 334.34 | **1.317×** | **321.61**（`BPSM` 6→5） | **1.267×** |
  | 15 | 273.48 | 447.21 | **1.635×** | 447.21（**ノブ無し**） | **1.635×** |
  | 31 | 356.08 | 789.33 | **2.217×** | 789.33（**ノブ無し**） | **2.217×** |
  | 63 | 409.37 | 836.40 | **2.043×** | 836.40（掃引点は +2.51%） | **2.043×** |
  | 127 | 592.22 | 1455.74 | **2.458×** | **1390.88**（y `BPSM` 2→1） | **2.349×** |
  | 255 | 882.29 | 2006.05 | **2.274×** | **1811.56**（`MINB` 3→2） | **2.053×** |

  **動くのは 3 次数だけ。** p=15 / p=31 は `minBlocks` が既に 1（＝そのスレッド数
  でのレジスタ上限）かつ spill 0 なので**共有が無料**。p=63 は 16 B を消せるが、
  y の占有率が 2 → 1 に落ちる代金（賞金は `LDL`+`STL` 6 本）が上回って **+2.51%**。
  取引は 4 次数とも同型で、p=63（6 命令、代金 −50%）と p=127（87 命令、代金 −50%）
  が**同じ代金・違う賞金で符号が反転する対照**になっている。
- [x] ~~数値が動かないことの確認~~ 点変化係数の owned `dqdt` 全点で、
  単体最適形は iso 形と**全次数ビット一致**（p=255 は `Ne=1` と `Ne=2³`、
  参照は `GEMM`、他は `SPLIT`）。ノブ `DFMA_OWN_BPSM` は既定 0、
  既定ビルドの SASS は改修前とバイト一致、production dispatch 外。
- [x] ~~**`ncu` を当てていない**~~ 2026-09-03 完了（job `78612`、node `c186`、
  commit `63803da` ＋ アブレーションノブ追加、`--metrics` 39 個、`-s 3 -c 2`）。
  3 点とも測定値に置き換えた（`dfma_register_budget.md` §10）。
  - (a) **p=7 と p=63 は外、p=127 と p=255 は中**（§10.1）。SASS の後ろ向き分岐
    区間と `smsp__inst_executed_op_local_*` の両方で示した。1 ワープあたりの
    実行回数は p=7 `LDL`/`STL` 4.0/4.0（静的 4/4、SASS に後ろ向き分岐が無い）、
    p=63 y 3.0/3.0（同）、p=127 xz **100/112**（静的 14/26）、
    p=127 y **397/223**（静的 52/35）、p=255 DIR1 **422/254**（静的 29/29）。
    **§4 の説明は結論としては正しいが変数が誤っていた** —— 効く量は静的本数
    ではなくループ内の実行回数で、p=63 対 p=127 の賞金差は 14 倍ではなく
    **100 倍**（6 回対 620 回）である。
  - (b) **賞金の stall は次数で違い、代金の stall も次数で違う**（§10.2）。
    p=7 は L1/TEX 93.4% 飽和で `long_scoreboard` 4.43→2.80、代金はどの stall
    にも見えない。p=63 y は `mio_throttle` 7.58→3.31 と
    `short_scoreboard` 5.11→0.70 を得るが、**代金が
    `long_scoreboard` 0.05→0.75 と `lg_throttle` 0.04→0.29 に出て負ける**。
    p=127 y は spill トラフィックが global の **4.2 倍**（local 1.05e8 対
    global 2.52e7 セクタ）で命令 −22.4%、代金は `math_pipe_throttle`
    0.51→1.41。p=255 は `short_scoreboard` 2.43→0.57、代金は
    `dispatch_stall` と `math_pipe_throttle` の微増のみ。
    対照の `p63_xz`（spill 0）は iso / opt で 37 メトリクス全同値。
  - (c) **p=127 xz の残存 112 B は 1 stage の 3.29%（直接測定）〜 ≈5.8%
    （線形外挿）**（§10.3）。数値不正なアブレーション
    `P127_XZ_ABLATE_AZ=8`（`az[16]`→`az[8]`、mma / shared / global は
    1 本も減らない、spill 112→48 B）で **−3.29%**（12 ラウンド交互、
    レンジ非重複、global セクタも DRAM バイトも不変）。spill を実質ゼロに
    する `PF=1` 併用は −9.59% だが **global セクタ −35% を巻き込む上限値**。
    交絡の無い 100% 除去点は測れていない。
- [x] ~~**残った判断: `AGENTS.md` の `FUSED_DFMA` の定義を変えるかどうか。**~~
  **2026-09-03 決定: 定義は変えない。論文は A_iso と A_opt の 2 列を載せ、
  2 つの値の違い自体を議論の対象にする。** 主比 A は従来どおり iso 分母。
  根拠は 3 つ ——(1) 一次的な成果は 2 列の差そのもの（「TC 向けの日程は DFMA には
  予算が足りない」）で 1 列にすると消える、(2) **6 次数のうち 3 次数で差ゼロ**
  なので下駄ではなく**次数依存の実費**であり、その機構は §10.1 の
  「spill がループの中か外か」で説明できる、(3) iso-schedule の列を失わない。
  棄却したのは脚注案（分布が脚注に潰れる）、定義変更案（列そのものを失い、
  しかも p=127 xz の 112 B は定義を変えても消えない）、役割外の列を別立てする案
  （付録向きで主表に置けない）。但し書きは **A_opt も代金を全部は除いていない**
  こと（p=127 で 3.29〜≈5.8% 残る）。詳細と再現手順は
  `dfma_register_budget.md` §8。`DFMA_OWN_BPSM` は既定 0・dispatch 外のまま、
  ソースの既定値は変えていない。
- [x] ~~未探索: DFMA 専用にスレッド数まで組み直した場合の到達点~~
  **2026-09-03 完了**（job `79187`、node `c385`、tree `ba47e6d` ＋ 役割外ノブ
  `DFMA_OWN_SHAPE`（既定 **0**、production dispatch 外、既定ビルドの SASS は
  差分ゼロ）、12 ラウンド交互、`dfma_register_budget.md` **§11**）。
  **日程が同一でなくなるので A_iso にも A_opt にも入らない役割外の値**であり、
  **`AGENTS.md` の定義は変えていない。**
  妥当性の境界: ブロックタイル・shared パネルと swizzle・チャンク深さ・
  二重バッファ・grid・スレッド内の総和順序は**全設定で不変**で、変えたのは
  **出力タイルをワープ／スレッドに切り分ける比率だけ**（§11.1）。
  - **p=127 xz の 112 B はスレッド数でも消えない。** 9 通りのワープ格子を
    ptxas に通して **spill 0 に届くものは 1 つも無い**（§11.2）。最小は
    **2×8（256 スレッド）の 48 B**、次いで 1×4 の 80 B / 1×8 の 96 B で、
    128 スレッドまで落とすと 1552〜2760 B と桁で増える。需要（アキュムレータと
    prefetch）はスレッド数に反比例して**倍**になるのに、供給は 128 → **255 で
    頭打ち**になるためである。**2×8 と 4×4 は同じ 256 スレッド・同じ
    アキュムレータ本数なのに 48 B と 464 B に分かれる**ので、残りは容量では
    なく ptxas の割り付けの問題である（理由は測れていない）。
  - **その 48 B 形は §10.3 の数値不正アブレーション `AZ=8` と同じ 48 B に、
    数値を保ったまま到達する。** 実時間は **−5.96%**（`s4`、xz だけを変えた版、
    レンジ非重複）。ただし `AZ=8` の −3.29% とは**同じ量ではない**
    （`AZ=8` は 512 スレッドのまま、2×8 は 256 スレッドで占有率も動く）ので、
    §10.3 の「1 stage の 3.29〜≈5.8%」は**更新されない**。
  - **spill 0 に届いた形とその時間**: p=127 y **8×4（128 スレッド、
    `minBlocks=2` を保ったまま 253 reg / 0 B）**、p=63 y `WN`=2（256 スレッド、
    128 reg / 0 B）、p=7 `KP`=4（64 スレッド、156 reg / 0 B）。
    **`minBlocks` の道には無い「占有率の段を払わずに spill を消す」選択肢**が
    スレッド数の道にはある。時間は p=127 が xz 2×8 と併せて **−18.97%**
    （うち y が −13.84%）、p=63 y が **−3.94%**、p=7 が **−2.03%**。
  - **次数ごとの到達点**（対 iso、12 ラウンド交互・全点レンジ非重複）:

    | p | DFMA iso | **スレッド数まで開いた最良** | 形 | 対 iso | 参考 `minBlocks` のみ |
    |---:|---:|---:|---|---:|---:|
    | 7 | 335.14 | **322.07** | `BPSM`=5（スレッド数は不変） | **−3.90%** | 同じ点 |
    | 63 | 841.29 | **800.17** | **xz `WN`=2** | **−4.89%** | 862.09（**+2.47%、負け**） |
    | 127 | 1456.99 | **1180.57** | xz 2×8 ＋ y 8×4 | **−18.97%** | 1392.29（−4.44%） |
    | 255 | 2018.38 | **1822.23** | `MINB`=2（スレッド数は不変） | **−9.72%** | 同じ点 |

    **超えるのは p=63 と p=127 だけ。** p=7 は `KP`=4 が spill を 0 にしても
    −2.03% しか買わず `BPSM`=5 の −3.90% に負ける。
    **p=255 にはスレッド数だけの道が無い**（形を変えると 5 通りとも spill が増える）。
  - **最も予想外だったのは p=63 の最大の勝ち −4.89% が、production で
    spill を 1 バイトも持たない `p63_xz` から出たこと**である。
    §5 の「TC の予算を着せられている代金」という枠組みでは説明できない。
  - 数値検証: `SCALE_DG_VARYING_COEFF=1`、owned `dqdt` 全点。
    p=7 / 63 / 127 は `CUDAFORTRAN_SPLIT`、p=255 は `CUDAFORTRAN_GEMM` 対照で
    max abs 1.78e-15 / 2.66e-15 / 3.55e-15 / 3.55e-15（相対 ≤4.16e-16）、
    **6 版すべて iso 形とビット一致**（総和順序を変えていないため）。
    p=255 の `Ne=2³` スモーク（134 217 728 点）も通した。
  - **2 列方針（§8）への含意は §11.7 に材料として置くだけで、決定はしていない。**
    但し書き（A_opt にも代金が残る）は**スレッド数まで開いても消えない**が、
    残量は 112 B → 48 B に減る。一方 A_opt を「DFMA で書いたら実際に到達
    できる速度」と読むなら、**第 2 引数だけ開いて第 1 引数を固定する理由は無く**、
    両方開いた比は p=63 で −7.2%、p=127 で **−15.2%** 低い。
    「動くのは 3 次数だけ」は `minBlocks` の軸についての事実である。
- [ ] **未測定として残ったもの**（`dfma_register_budget.md` §11.8）。
  (a) **`ncu` を当てていない**ので、p=63 xz の −4.89%（両側とも spill 0）、
  p=127 y 8×4 の −13.84%（SM あたり 8 warp しか無い形が 32 warp に勝つ）、
  p=127 xz の 2×8 と 4×4 が同条件で 48 B と 464 B に分かれる理由は、
  **どれも機構が測れていない**。
  (b) p=63 の xz と y を**同時に**変えた版（加法性は未確認）。
  (c) p=255 の 2×4 ＋ `MINB255_DFMA=2`（spill 0–8 B に届く唯一の点。
  コンパイルだけで**時間を測っていない**）。
  (d) p=7 の `KP`=4 × `BPSM` の掃引（6 / 8 / 12 しか見ていない）。
  (e) **p=15 / p=31 / p=511 のスレッド数掃引は 1 点も無い。**
  §5.3 の「p=15 / p=31 は共有が無料」は **spill についてしか成り立たない**
  ことが p=63 xz の例で分かったので、この 2 次数は再び開いている。
  (f) 交絡の無い「p=127 xz の spill 100% 除去」点は依然として存在しない。

## 3. 「未着手 / 未実装」と明記されているもの

| 経路・次数 | 項目 | 出所 | 天井 |
|---|---|---|---|
| [x] ~~p=15 `FUSED_TC`~~ | ~~面点 M 側を **shared 経由で再配布**~~ **2026-09-02 完了、不採用 +3.44%**（`p15_gap_study.md` §30、job `78090`/`78092`） | `p15_gap_study.md` §14.6 → **§16.6 で既に 7 割実装済みだった**（本 TODO 行の出所が陳腐化していた） | 天井は現行 HEAD で **−6.68%**（18.2 µs、272.5 → 254.3）。§14.6 の −7.6%（26 µs）ではない |
| [x] ~~p=31 `FUSED_TC`~~ | ~~xz ld 4.32% / y st 16.34% のバンクコンフリクト~~ **2026-09-02 完了、天井 0% で閉じた**（`p31_gap_study.md` §32、job `78090`/`78093`、ncu `78092`） | `p31_gap_study.md` §14.6「**不採用ではなく未着手として残す**」 | **0%**（測定値。4 版レンジ重複） |
| [x] ~~p=255 `FUSED_TC`~~ | ~~`BK=32`（バリア半減）~~ **2026-09-03 完了、不採用 +11.00%**（`p255_gap_study.md` §26.3、job `78165`、ncu `78172`） | `p255_gap_study.md` §7「**未実装**」→ §16 / §17 が既に近い形を測っており、本 TODO 行の「未実装」は **swizzle を一般化した正しい実装が無い**という意味だった | **−0%**（測定値。`MINB=2` の上で `BK=16` → 32 は +1.10%、バリア stall 1133 → 1074） |
| [x] ~~p=255 `FUSED_TC`~~ | ~~**x と y の融合**~~ **2026-09-03 完了、不採用（数え上げで閉じた）**（`p255_gap_study.md` §26.4、job `78175`） | 同 §7・§9「**未実装**」 | **−1.68%（14.9 µs）**（tight アブレーション実測。x のストア −1.47% ＋ y の読み −0.20%）に対し、実装可能な唯一の予算 `MINB=2` の代金が **+9.79%（86 µs）** |
| [x] ~~p=255 `FUSED`~~ | ~~z の D の global 化~~ **2026-09-03 完了、不採用 +11.26%**（`p255_gap_study.md` §26.5、job `78165`、ncu `78172`） | 同 §15「z は y と同型のため**未実装**」 —— 同型という見立ては正しかったが、測るまで理由は分かっていなかった | **負**（shared WF −50% と引き換えに global request **+421%**、`lg_throttle` ×11.9） |
| [x] ~~p=7 `FUSED_TC`~~ | ~~`m16n8k*` FP64 mma~~ **2026-09-03 完了、不採用 +24.4%（役割外アブレーション）**（`tc_paper_survey_2407.09621.md` §19、job `78202`、node `c384`） | `tc_paper_survey_2407.09621.md` §7.5「**未着手**」（§2 B-1）→ **B-1 の前提が GB200 では偽だった**（wider な FP64 MMA は sm_90 までの native 命令で、sm_100 の ptxas は `DMMA.8x8x4` に 2/4/8 本展開する） | **0%**（測定値。`P7_BPSM=4` に揃えると `m8n8k4` 339.91 対 `m16n8k8` 340.05 でレンジ完全重複、SASS はバイト一致） |

**p=7 の行（完了）**: 経路定義の判断は **(b) 役割外アブレーション**とした ——
`AGENTS.md` は `CUDAFORTRAN_FUSED_TC` の定義文に `mma.sync.m8n8k4` を
**命令名で**書いており、同じ節が「経路定義の変更には明示的な編集が要る」と
定めているため。ノブ `P7_TC_M16N8K8`（既定 0）としてツリーに残し、
production dispatch には出していない。
ただしこの判断は**実質的に空回りした**: sm_100 では `m16n8k8` と書いても
発行される機械語が `DMMA.8x8x4` のままなので、(a) と読んでも定義は破れない。
測ったこと 4 点 —— (1) `m16n8k4/8/16` は sm_90 では native な
`DMMA.16x8x*` だが sm_100/100a/103/120 では `DMMA.8x8x4` × 2/4/8 に展開される、
(2) 4 形とも 39 TFLOP/s（ピークの 97%）で差が無く、`m16n8k8` は B レジスタを
共有させた 4 本の `m8n8k4` と時間もレジスタ数（48）も一致する、
(3) p=7 に差し替えた版の SASS は `m8n8k4` 版と**バイト一致**、
(4) それでも ptxas は 52 レジスタ以上を要求して
`__launch_bounds__(256, 8)` を**エラーで拒否**し、`P7_BPSM=4`（占有率 50%）
まで落とさないとビルドできない。その代金が **+24.4%**。
**副産物（未決）**: `P7_BPSM=4` にすると `FUSED_DFMA` は逆に **−6.13%**
（424.29 → 398.26）。production の 32 レジスタでは DFMA 側だけが 80 B
スピルしており、iso-schedule のために TC のレジスタ予算を共有させられている
代金である。§2.3「副産物 2」（p=255 の DFMA スピル）と同じ族の 2 例目で、
p=7 では時間まで測れた。機構比 A はスピル無しの分母なら 1.457×（§18 は 1.551×）。

**p=15 の行（完了）**: §14.6 が「未実装」と書いた案は §16.6 が i 法線 2 面ぶんを
**−6.1% で採用済み**だった。本 TODO が引いていた天井 −7.6%（26 µs）はその回収前の
値である。残っていた j/k 法線 4 面（48 KB）を実際に書くと、global request −13.9% /
sector −13.4% / `long_scoreboard` −21.9% と狙いは当たるのに、命令 +11.8%・
shared wavefront +11〜15%・carveout +32 KB が上回って **+3.44%（レンジ非重複、
改修前とビット一致）**。天井 ablation（M 側全面 coalesced）は **−6.68%**。

**p=31 の行（完了）**: xz の shared ld コンフリクトを **94.7% 消し**（4.31% → 0.23%、
ncu job `78092`）、swizzle の整数演算ごと **命令 −6.3%** にしても、global request も
sector も DRAM バイトも 1 つも動かず、実時間は **+0.12%（レンジ重複）**。
y の st コンフリクトは 2/3 が 128-bit shared ストアに固有でアドレスでは消えず、
残り 1/3 を消す唯一の道（`sDQ` 撤去）は `long_scoreboard` 2.01 → 11.67 で **+0.62%**。
§30 の「p=31 xz は shared でも命令でもなく global と DRAM で詰まっている」を
**符号の逆側から**裏づけた 8 例目である。

~~`BK=32` と x/y 融合はどちらも**レジスタ予算が理由**で止まっている
（プリフェッチ 48→96 で `MINB=3` に入らない / 蓄積器 2 組で 224 超）。
2.1 の「占有率を下げて予算を作る」が効けば、この 2 つも再検討の対象になる。~~

**p=255 の 3 行（完了、2026-09-03、`p255_gap_study.md` §26）。**
まず ptxas でレジスタ予算の壁を出した: TC の `tendency_p255_kernel` は
本番 `BK=16`/`MINB=3` で **168 レジスタ・spill 0**、`MINB=2` で 208、
`BK=32`/`MINB=2` で **255・spill 0**（動的 64 KB）、`BK=32`/`MINB=3` で
168 に押し込まれて 100 B spill。**壁は 255 レジスタで、`BK=32` はそこに収まる**
——「予算が理由で止まっている」という本 TODO の記述は、**予算が足りないのでは
なく予算の値段が高すぎる**、が正しかった。`MINB=2` を買うだけで **+9.79%**
かかり、買った `BK=32` が返すのは **−0%**（バリアはもともと律速ではなく、
本番形でも barrier stall 3370 に対し `math_pipe` 21450 / `wait` 17870）。
x+y 融合の賞金も tight アブレーションで **14.9 µs** しかない。
したがって **2.1 の「占有率を下げて予算を作る」は p=255 TC では成り立たない**
（`p31_gap_study.md` §30.5 に続く 2 例目の反例。効いた p=127 xz は
`long_scoreboard` 11.37 の global 待ちで、隠す相手があった）。

**副産物 1（採用、−18.63%）**: 同じ点検で **出力タイル (A) × レジスタ予算 (B)
の次の段が p=255 CC で未適用**だと分かった。この軸は §15.68–§15.76 が発祥で
1 段目（8 出力 / 64 レジスタ）は入っていたが、p=63 / p=127 が 2.2 で到達した
128 レジスタ・占有率 24% に対し p=255 CC は **64 レジスタ・占有率 50%** のまま
だった。x 8i×4j / `minBlocks=3`、y 8i×4j / `minBlocks=2`、z 8line×4k /
`minBlocks=2` にして **1874.3 → 1525.0 µs/stage（−18.63%、job `78165`、
12/12、レンジ非重複、`Ne=1` / `Ne=2³` ともビット一致）**。主比 B は
2.11× → **1.73×**。機構は p=63 §53 / p=127 §24 と同じ署名で、
**DRAM バイトは 1 バイトも動かない**。梯子は `TI=16` で終わる
（y は静的 shared 48 KB 超、x/z は +4.7〜+22%）。

**副産物 2（未評価）**: 本番 `BK=16` でも **`FUSED_DFMA` 側（`UseTc=false`）は
168 レジスタで 56–124 B の spill を出している**（TC 側は 0）。§2.3 が
「p=255 だけ DFMA が +0.95% 遅い」を断定保留にした点の一因候補。
時間への寄与は未測定（§4 に挙げるほどの未説明ではないが、記録として残す）。

## 4. 未説明の測定（`AGENTS.md` の基準ではモデルの欠陥）

- [x] ~~**p=7 `FUSED_TC` の shared store コンフリクトが 2.41M → 4.10M に増える理由。**~~
  2026-09-03 完了、`tc_paper_survey_2407.09621.md` **§20**（job `78215` / `78216`）。
  **増分はコンフリクト率ではなくストア要求数**である: ステージングは 1 ブロックあたり
  64 要求（+80%）を足し、`l1tex__..._op_st` は「wavefront − 最小 wavefront」なので
  64-bit ストアの最小充当 2 wavefront/要求ぶんだけ分母が動く。実測の最小充当差は
  ちょうど 128 = 2 × 64。**率は 29.60% → 30.14% で不動**、1 要求あたりの実 wavefront も
  2.82 → 2.85 で不動。`sMface` のレイアウトは設計どおりで、他のストアも同様に
  **アドレスをレーン線形にしても計数は減らない**（3 群のうち 2 群は増え、
  Δコンフリクト = Δwavefront が 1 の位まで一致）。§32.6 の 128-bit ストアと同族。
  **時間への寄与は負**: 3 群すべてレーン線形化で命令 −8.4% でも **+2.67%**
  （job `78216`、c386、12 ラウンド、レンジ非重複）。§13.2 のステージング自体は
  現行 HEAD でも **−5.9%**（`NOSTAGE` +6.30%）で黒字。§13.5 に訂正注記を入れた。
  **残っていた「1 要求あたり 2.8 wavefront」の出所も 2026-09-03 に閉じた**
  （`tc_paper_survey_2407.09621.md` **§24**、job `78531`、node c187、
  マイクロベンチ `scripts/micro_shared_wavefront.cu`）。
  (1) 「64-bit の最小充当は 2」という**見積もりは正しい**（レーン線形の
  64-bit ストアはちょうど 2.000・コンフリクト 0、32-bit は 1.000、128-bit は 4.000）。
  (2) **誤っていたのは要求数**で、§20.2 の 80 / 144 要求/ブロックは実測 **72 / 136**。
  よって **2.82 / 2.85 ではなく 3.03 / 3.16**、超過は **1.03 wavefront/要求**。
  (3) **出所は L1TEX Data-Stage の競合**。カウンタは「Data-Stage が処理した
  wavefront 数」＝サイクル計数で、**同一アドレス**のレーン線形 64-bit ストアに
  global ロードを重ねるだけで 2.000/0 → **2.150/39,348** になる。実カーネルでは
  超過が**占有率と global トラフィックの 2 変数で加法的に分離**し、ワープ内
  アドレスがビット単位で同一の `KP=1` → `KP=2` で **1.032 → 0.248**。
  §32.6 の「128-bit ストアに固有」という幅の説明は**反証**（レーン線形 128-bit は
  4.000・コンフリクト 0）、正しくは**同族で、共通項は L1TEX の競合**。
  未測定として残るのは、超過の定量モデル（マイクロベンチの超過 0.150 は
  実カーネルの 1.03 の 1/7）、超過サイクルの正体（再発行か調停か計数粒度か）、
  p=31 y カーネルの再測定の 3 つ（§24.7）。天井は負のままなので採否は動かない。
- [x] ~~**p=31 `FUSED_TC` の面 2,4 が 5 形すべて負ける理由。**~~
  2026-09-03 完了、`p31_gap_study.md` **§33**（job `78215` / `78216`）。
  天井は HEAD でも有効（`F24=1` **−17.98%**、§18.2 の −17.8% の再現）。
  **5 形が取りにいっていた賞金は −4.71% しかない**: M 側だけ coalesce −4.71%、
  M 側を消す −4.29%、**P 側だけ coalesce −2.53%**、**両方 −17.22%** と
  強く非加法。理由は DRAM バイトで、片側だけでは 1156.8 → 1124.0 / 1144.1 MB
  （−2.8% / −1.1%）しか動かず、両側で 851.2 MB（−26.4%）に落ちる。
  launch 全体では M 側が引くラインと P 側が引くラインが同じ集合（どの要素の
  `i=0`/`i=31` 列も自分のブロックの M 側かつ隣 2 ブロックの P 側）なので、
  **片側を直してもラインは隣が要求し続ける**。P 側は隣接要素でブロック内に無い。
  帰属表は §33.4: **§26 で全額説明できるのは 5 形目だけ**（stride 32 のままなので
  賞金ゼロ、代金は `VMapM` の MLP。HEAD 再測 +1.50%）。1〜4 形は §26 では説明できず、
  賞金 −4.71%（DRAM −2.8%）に対して 2〜4 形は DRAM を +12〜44% 足し、
  1 形目はワープ効率を 2/32 にする。§30 / §32 との整合は §33.5。

## 5. レポートが沈黙している判断

いずれも「やらない」と決めた記録が無い。決めたなら記録し、決めていないなら測る。

- [x] ~~**p≥511（511 / 575 / 767 / 1023）に融合経路が存在しない理由。**~~
  **2026-09-03、4 次数とも完全に閉じた。** §14 が 575 / 767 / 1023 を数え上げで
  閉じ、開いたまま残した p=511 を **[`p511_gap_study.md`](reports/p511_gap_study.md)
  §15 が実装して測って閉じた**（job `78411`、node `c182`、親 `e9d1037`）。
  **`tendency_p255_kernel` を `NQ` でテンプレート化して p=511 に
  `CUDAFORTRAN_FUSED_TC` / `CUDAFORTRAN_FUSED_DFMA` を開通させた**
  （`NQ255` 直参照 6 か所、`extern "C"` 2 本に `nq`、Fortran のゲート、
  namelist 5 本）。ptxas は `NQ=512` でも **168 レジスタ / spill 0 /
  32,768 B** と `NQ=256` に一致し、**§14.3 の資源の数え上げは実装で確認された**。
  点変化係数で owned `dqdt` 全 **134,217,728 点**を `GEMM` 対照で
  max abs **3.553e-15**（TC と DFMA はビット一致、`NQ=256` は改修前とビット一致）。
  - **占有 GPU 12 ラウンド交互、全レンジ非重複**（物差しは `CUDA device *` ÷ 54）:
    `GEMM_FUSED` **12,406.7** / `FUSED_TC` **13,084.0** / `GEMM` **13,264.7** /
    `GEMM_CUTE` **13,382.1** µs/stage。**`FUSED_TC` は `GEMM_FUSED` に +5.46%
    負け、`GEMM` / `GEMM_CUTE` には −1.36% / −2.23% 勝つ。**
    **p=511 の最速は `GEMM_FUSED` のまま**なので README の最速経路表と主比は
    動かしていない。経路は既定で dispatch 可能なまま残した（役割内の、
    負けた本番経路であって役割外アブレーションではない。§15.8）。
  - **§14 の帰趨: 数え上げは維持、外挿だけ訂正。** 賞金 `volume_flux` は
    実測 **8.36%**（§14.4 の 8.30%）、損益分岐は実測 **83.4%**
    （§14.5 の 82.9–83.4%）。**外れたのは「`FUSED_TC` のリードを 6 ポイント
    足して −6.8%」という外挿**で、実測のリードは **−4.3 ポイント**。
    リードは Nq=256 と Nq=512 の間でゼロを横切る。
  - **機構は nsys 分解が全額説明する**: 融合が吸収する `volume_flux` が
    **−1.038 ms/stage**、手書き mainloop 3 本が CUTLASS の 3 本より
    **+1.711 ms**（mma 屋根の **80.0%** 対 **88.6–95.5%**）、差引 +0.674 ms
    ＝ **+5.54%** で実測 +5.46% と 0.1 ポイント一致。`FUSED_TC` の対ピーク比は
    73.7 → **79.1%**（+5.2 pt）と減速し、損益分岐は 67.6 → **83.4%**（+15.8 pt）
    と減速しない。**手書き mainloop が自分の屋根に着き、CUTLASS はまだ伸びる。**
  - **p=575 / 767 / 1023 の「書かない」は維持**（根拠が外挿から実測に変わった）。
    **CC 融合も 4 次数とも書かない**（p=255 CC はピークの 45.2% で 83.4% の
    1.85 倍下。**p=511 の CC は測っていない**と明記）。
  - [x] ~~**残る未説明**: `Nq=256` での同じ nsys 分解（手書き mainloop の屋根が
    p=255 でも 80% なのか）は本作業では測っていない。~~
    **2026-09-03 に閉じた**（[`p255_gap_study.md`](reports/p255_gap_study.md)
    §29、tree `44b02a6`、job `78530`、node `c187`）。**答えは「80% は次数に
    よらない定数ではない」。** 3 本平均の対 mma 屋根は
    手書き **75.2%（Nq=256）→ 80.1%（Nq=512）**、
    CUTLASS **83.1% → 92.5%** で、差は **7.9 → 12.4 pt に広がる**。
    したがって §15.6 の「手書きは屋根 80% で頭打ち」は**訂正**で、正しくは
    **賞金が `1/Nq` で縮み（14.16% → 8.36%）、効率差が広がる 2 本の曲線の交差**。
    Nq=256 の帰属は 賞金 −14.16% ＋ 超過 +8.70% ＝ **−5.46%** で、
    実測 wall −6.36%（`FUSED_TC` 880.5 対 `GEMM_FUSED` 940.3 µs/stage、
    12 ラウンド交互、レンジ非重複）と 0.9 pt 一致（残差は 3 本の外の
    launch 間隙 10.4 µs で説明がつく）。**p=255 の `FUSED_TC` の勝ちは
    mainloop ではなくエピローグ融合が消す 1 本のカーネルによる**
    （3 GEMM 部分だけなら CUTLASS に 10.1% 負けている）。
    なお 6 次数リード表の p=255 行は現行ツリーで **69.3 / 73.9 ＝ +4.7 pt**
    （§15.6 当時は +6.1 pt）で、**交差点は §15.6 のときより Nq=256 側に寄っている**。
  以下は §14 の当時の記録である。記録先は
  [`p511_gap_study.md`](reports/p511_gap_study.md) **§14**
  と `reports/README.md` の表注（新規測定なし。既存の実測値と、本件のために
  取り直した ptxas / `cudaGetDeviceProperties` の数え上げ）。
  **数え上げの答えは「不可能」ではなかった。**
  - **要素常駐型（p=7…127 の様式）は不可能**: 1 枚の `Nq×Nq` 平面が
    shared/block 上限 232,448 B に対し Nq=256 で **2.26×**、Nq=512 で **9.02×**、
    Nq=1024 で 36.1×。Nq=256 で既にあふれているのが、p=255 `FUSED_TC` が
    タイル型になっている理由である。加えて p≥511 は `Ne=1` なので
    「1 ブロック = 1 要素」の grid は **152 SM に対し 1 ブロック**。
  - **タイル型（p=255 の `tendency_p255_kernel`）はそのまま載る**:
    shared **32,768 B**・レジスタ **168**（spill 0）・128 スレッド・
    **3 ブロック/SM**（占有率 18.75%、律速はレジスタファイル）は
    **どれも `Nq` を含まない**。`Nq` が入るのは grid（4,096 → 32,768 /
    46,656 / 110,592 / 262,144、上限の 0.012% 以下）とチャンク数
    （16 → 32 / 36 / 48 / 64）だけ。メモリも融合の方が少ない（64.5 対
    145.3 B/node）。Nq=1024 でだけ `Escale` 方向オフセットの 64-bit 化が要る。
  - **止めているのは賞金**: 融合が `GEMM_FUSED` から削れる仕事は
    `volume_flux` 1 本だけである（assembly/lift は既に z のエピローグに融合済み、
    `elembnd_flux` は side stream で完全に隠れ、カーネル間の隙間は 0.02%、
    `rk_update` は tendency の外）。その stage 比は `1/Nq` で落ちる ——
    **8.30%（p=511、§11.2 の nsys）→ 4.47%（p=1023、`p1023_gap_study.md`
    §13.2 の nsys）**。モデル `8.30%×512/Nq` は p=1023 で 4.15% を出し実測と 1.08 倍。
  - **損益分岐効率 = `README.md` の `vs 40.1` 列そのもの**（82.9 / 85.6 / 86.6 /
    88.0%）。`FUSED_TC` の最高実績は p=255 の **73.7%**、CC は **42.7%**。
  - **判断**: **p=575 / 767 / 1023 は「書かない」と決めた**（天井 4.5〜5.5%、
    しかも p=1023 では `volume_flux` を critical path から外す不正アブレーションが
    0.10% しか返さないことが実測済み。損益分岐 86.6–88.0% は実績 73.7% の
    13–14 ポイント上）。**CC 融合は 4 次数とも書かない**（42.7% は分岐の半分）。
    **p=511 だけは決めずに残す**（天井 8.30%、損益分岐 82.9–83.4%、
    リード外挿 −6.8%）。**本行は p=511 についてだけ未着手として開いたまま
    残す**。手順の見積もりは `p511_gap_study.md` §14.6 にある
    （`tendency_p255_kernel` の `NQ` テンプレート化 —— `NQ255` は 6 か所 ——、
    Fortran 側のゲート、`perf_p511_fused_tc.conf`、点変化係数での全点検証）。
- [x] ~~**p=7 / p=15 への PDL 適用可否。**~~
  2026-09-03 完了、**適用不能**。記録先は p=7 TC
  [`tc_paper_survey_2407.09621.md`](reports/tc_paper_survey_2407.09621.md) **§22**、
  p=7 CC [`p7_gap_study.md`](reports/p7_gap_study.md) **§6**、
  p=15 [`p15_gap_study.md`](reports/p15_gap_study.md) **§32**
  （nsys job `78241`、node `c182`、graph A/B は login 5-run）。
  - **p=7 / p=15 の tendency は TC も CC も 1 グリッド**である
    （`tendency_fused_p7_kernel<<<Ne,…>>>` ほか 1 本に面フラックス・3 微分・
    エピローグが全部入る）。**p=127 の梯子（面 → xz、xz → y）の段が 1 つも無い。**
  - nsys の 1 stage は `rk_update`（OpenACC）→ `update_halo`（OpenACC）→
    tendency（CUDA C++）の **3 本だけ**。CUDA C++ グリッドは 1 本で、前後の隣は
    OpenACC 生成なので `cudaLaunchAttributeProgrammaticStreamSerialization` を
    付ける先が無い。一次側が Fortran では重ならないことは
    `p127_gap_study.md` §19.2 が実測済み。
  - **狙える隙間の天井を実測した**: 隙間合計は p=7 **9.81 µs = stage の 2.76%**、
    p=15 **9.73 µs = 2.74%**（stage 周期 355.20 / 355.33 µs は login の
    `Step loop per stage` と 0.01–0.12% で一致）。そして
    **`UseCudaGraph = .true.` がそれを全額回収する**:
    p=7 TC 355.24 → 345.13（**−2.85%、−10.11 µs**）、p=7 CC 374.21 → 364.02
    （**−2.72%、−10.19 µs**）、p=15 TC 355.75 → 345.20（**−2.97%、−10.55 µs**）、
    p=15 CC 416.51 → 406.26（**−2.46%、−10.25 µs**）。いずれもレンジ非重複。
    **PDL がその上に足せる分は 0。**
  - 段を作る分割案も負け: p=7 は 32,768 ブロック / 8 per SM / 152 SM で **27.0 wave**、
    p=15 は 4,096 ブロック / 1 per SM で **27.0 wave**。tail は最良でも 1/27 = 3.7%
    しかなく、分割は融合が消した global 往復を 1 本戻す。
  - 副産物: 取れる µs（10.1〜10.6）は**経路によらずほぼ一定**で、1 stage の
    カーネル本数（3 本）で決まる。`GEMM_FUSED` の graph 利得 22〜26 µs
    （`README.md`、6 本前後）と整合し、「得は µs でほぼ一定、率は stage の長さで
    決まる」という見立てを本数側から裏づける。**既定 conf は graph off のまま。**
- [x] ~~**p=7 `FUSED_TC` にチャンクループがあるかどうか。**~~
  2026-09-03 完了、**チャンク数は p=7 も p=15 も 1**。符号則は棄却を予測し、
  そのとおり記録して閉じた。記録先は p=7
  [`tc_paper_survey_2407.09621.md`](reports/tc_paper_survey_2407.09621.md) **§21**、
  p=15 [`p15_gap_study.md`](reports/p15_gap_study.md) **§31**。
  - **p=7: 縮約ループがそもそも存在しない。** 3 方向の流束パネルが全部 shared
    常駐（静的 20,864 B、32 レジスタ、spill 0）で、`Nq=8` の縮約は
    `mma_k8_8x8()` の中で `mma.sync.m8n8k4` **2 本を直に並べる**だけ。
    `for` も staging ループも無い。
  - **p=15: `BK = Nq = 16` で 1 チャンク。** 3 パネル（各 4096 double、
    動的 113,408 B）が同時常駐し、`for (ks=0; ks<4; ++ks)` は **mma の
    k ステップ**であって staging のチャンクではない。バリアは全体で
    `__syncthreads` 2 本 + `__syncwarp` 1 本。p=63 の `BK63 = NQ63 = 64` と同型。
  - **チャンクを新設する側の動機も無い**（占有率の数え上げ）: p=7 は
    `32 レジスタ × 256 スレッド × 8 ブロック = 65,536` ＝ レジスタファイル全部で
    **占有率 100%**、p=15 は `64 × 1024 = 65,536` で **1 ブロック/SM 固定**。
    どちらも shared を減らしてもブロックは 1 枚も増えず、得られるのは
    バリアの増加だけ。符号は確定して負。
  - **符号則の閾値はチャンク 2 個**と書けるようになった: 1 個は p=7 / 15 / 31 / 63 の
    4 次数とも負、2 個以上は p=127（−4.25%）/ p=255（−27.7%）とも正。
    低次数で 1 個になる理由も共通で、`Nq²·8 B` の平面が 227 KiB に収まるため
    そもそも縮約を切る必要が無い。

## 6. 古い可能性のある「探索終了」宣言

**覆った 5 例はいずれも同じ理由だった** ——「当時の候補集合に、後で他次数から来る軸が
入っていなかった」。したがって残る宣言も同じ理由で古い可能性がある。

覆った実績:

| 覆された | 覆した | 内容 |
|---|---|---|
| `p63_gap_study.md` §20 | §50.6 | 「§20 の『探索終了』は**当時の候補が尽きたという意味**」。PDL −3.0% |
| `p127_gap_study.md` §11.13 / §20.3 | §23.9 | 「**塞がっていたのは占有率であって性能ではなかった**」 |
| `p15_gap_study.md` §22.3 | §27 | 「(A)/(B) の軸を測っていなかったという意味で不完全だった」 |
| `p31_gap_study.md` §20.4 | §27.6 | 「**取り消す**。天井を測っていなかったのは (A) の側」 |
| `p255_gap_study.md` §16 | §17 | 「当時はここで探索終了と書いたが、§17 で残っていた」 |

覆した実績（2026-09-03 追記、上の 5 例に続く）:

| 覆された | 覆した | 内容 |
|---|---|---|
| （宣言が無かった）p=7 `FUSED_TC` | `tc_paper_survey` §23 | 出力タイル (A) を **TC 側**に当てて **−7.15%**。宣言はその後に出した |
| `tc_paper_survey` §14 | 同 §23.9 | 「32 レジスタに余白が無い」→ 予算 80 で測り直し、**やはり負け（+5.3%）**。前提は変わったが結論は変わらない |

再点検の結果:

- [x] ~~`p7_gap_study.md` §4（`FUSED`）~~ —— **すでに取り消し済み**（§5 の訂正注記、
  `51b1abf`）。本作業で二重には取り消していない。CC 側の現在地は 292.3 µs/stage。
- [x] ~~`p63_gap_study.md` §25.5（`FUSED`）~~ —— **すでに取り消し済み**（§53.9、`a25720e`）。
- [x] ~~`p127_gap_study.md` §17.5（`FUSED`）~~ —— **すでに取り消し済み**（§24.8、`a25720e`）。
- [x] ~~`p255_gap_study.md` §22（`FUSED_TC`）~~ —— **維持**（`p255_gap_study.md` §27.2、
  2026-09-03）。新軸である**出力タイル (A) の第 2 段をこの経路に当てて測った**:
  2 倍タイル（128×64 / 4×8）は 255 レジスタにちょうど収まるが、
  **iso 予算で +0.33%（返らない）**、予算 `MINB=2` の代金は +9.77%（job `78266`、
  12 ラウンド、全レンジ非重複）。機構は §22 の ncu どおりで、p=255 TC は
  L1/TEX 53–54%・shared コンフリクト 0.5% 未満・`math_pipe`+`wait` 律速なので
  **(A) が割る相手がどれも屋根ではない**。「占有率を下げて予算を作る」の反例 3 例目。
- [x] ~~`p255_gap_study.md` に `FUSED` の探索終了宣言が**無い**~~ —— **2026-09-03 に
  `p255_gap_study.md` §28.9 で出した。** §26.9 が残した 2 軸を順に閉じた:
  j タイル幅は §27.3（採用ゼロ。`TI×TJ=32` の 8×4 と 4×8 は差が無く 16×2 だけ
  +16%、64 出力の 8×8 も差なし ＝ **梯子は出力数で決まり i / j の配分では
  決まらない**。一般化コード自体が +0.79% なので HEAD に戻した）、
  **最後の 1 軸だったブロック形状は §28**（x / y / z それぞれ `BDX` = 4 / 8 /
  16 / 32 を掃引し、**y と z が `(8,16)`**（z は `minBlocks` 2 → 4 とセット）で
  **1525.0 → 1440.04 µs/stage、−5.60%**、job `78391`、node c186、12 ラウンド
  交互・全レンジ非重複、`Ne=1` / `Ne=2³` とも HEAD とビット一致。x は `(16,8)`
  据え置きで、`(8,16)` は **+2.24%**）。**§27.3 の規則はブロック形状には
  及ばなかった**: 1 スレッド内の `TI × TJ` の配分はブロックが覆う範囲を変え
  ないので出力数だけで効くが、ブロックの形は**フラックスの DRAM 読み直し回数**
  （＝「フラックスが依存しない方向のブロック数」）を変える。`BDX` 16 → 8 は
  y / z でそれを 8 → 4 に半減させ、x では 2 → 4 に倍増させる。z の
  `dram__bytes_read` は **2.425e9 → 1.347e9（−44.5%）**、L2 hit 10.5% → 33.3%
  （ncu job `78394`）。**占有率ではない**（`minBlocks` 2 → 4 の寄与は −0.55% で、
  本体の −4.47% は占有率 18% のまま得られる）。**ncu の Duration は逆符号に
  出る**ので、ncu の時間で採否を決めていたら落としていた（7 例目）。
  天井は §28.8 で 1441.25 基準に引き直した（INNER=1 −43.06% / D≡1 −14.74% /
  Q≡1 −12.83% / エピローグ剥がし −6.41% / バリア全削除 −3.78%、job `78396`。
  **§27.4 で入れ替わっていた D と Q の順位がまた入れ替わって元に戻った** ——
  Q の賞金は §28 が DRAM 側で現金化した分だけ縮み、D の賞金は shared 側の
  代金の分だけ増えた）。宣言は §28.9 に**候補集合の内訳つき**で書いてある
  （「2026-09-03 の時点で列挙できた候補が尽きた」という意味であり、
  将来他次数から来る軸で覆る可能性は否定していない）。
  主比 B は 1.73× → **1.630×**、最速経路は `CUDAFORTRAN_FUSED_TC` のまま。
- [x] ~~p=7 `FUSED_TC` にも探索終了宣言が無い~~ —— **2026-09-03 に
  `tc_paper_survey_2407.09621.md` §23.8 で 1 か所にまとめて出した。**
  出す前に、§19.10 が「未着手」と明記していた**出力タイル軸 (A)** を当てて
  **−7.15%**（`FUSED_DFMA` は −21.16%、job `78266`）、
  さらに本節の改修で前提が変わった §14（D1D フラグメントのレジスタ常駐）も
  測り直して再棄却した（§23.9、+5.3%）。**主比 B は 1.06× → 1.154×、
  機構比 A は 1.551× → 1.322×**（job `78271`）。

## 7. GEMM 系列で棚卸ししていない部分

`reports/gemm_assignment_and_carrier.md` §6 は閉じたが、それは**この一連の作業が
自分で開けた穴**であって、GEMM 系列全体の棚卸しではない。

**2026-09-03、全 5 行を測って閉じた。記録先は
[`gemm_assignment_and_carrier.md`](reports/gemm_assignment_and_carrier.md) §9
（Slurm job `78296` / `78297`、node `c384`、12 ラウンド交互）。**

- [x] ~~**`GEMM` パスの周辺カーネル融合。**~~ **採用**（§9.1）。`flux_all_kernel` を
  新設し、**GEMM 系 5 経路が共通で通る `launch_flux_all` 1 か所**から起動する
  （`launch_volume_flux` と同じ扱い）。既定 `SCALE_DG_FLUXFUSE=1` は
  「face flux が dg_stream 上で volume flux と直列になるときだけ融合する」で、
  **規則は 5 経路で 1 つ**、発火するかどうかは経路ごとの既存 schedule が決める。
  `GEMM` −0.066 / −0.171 / −0.279%（p=7/15/31、いずれも 12/12）、
  `GEMM_CUTE` −0.050 / −0.228%（p=7/15）、p=31 `GEMM_CUTE` は差なし、**負けは無い**。
  得は 1.1〜1.7 µs/stage で次数にほぼ依存せず、launch 1 本ぶんの定数として読める。
  **モード 2（side stream の重ねを捨てて必ず融合する）は 3 次数とも負ける**
  （`GEMM_FUSED` で +0.44 / +0.80 / +1.40%）＝ `p31_early_face` の値段を逆側から
  測ったことになる。全モード・全経路で owned `dqdt` はビット一致。
- [x] ~~**`GEMM` パスの 3 本並列。**~~ **採用、本項目で最大の勝ち**（§9.4）。
  `SCALE_DG_GVOLPAR`（既定 2 ＝ y を side2、z を side1、face flux が side1 に
  居るときは自動で y のみ）。**7 次数すべて 12/12・レンジ非重複**で
  **p=7 −1.23% / p=15 −1.89% / p=31 −1.56% / p=63 −1.60% / p=127 −2.82% /
  p=255 −2.03% / p=511 −0.22%**。
  **本 TODO が引いていた「高次数は赤字」は成り立たなかった**: 出所の
  `p575_gap_study.md` §11.1 / §11.7 は `flux_y`/`flux_z` を x GEMM の裏に回す
  別の話で、volume GEMM 同士の重ねではない。効きは**高次数側で最大**である。
  `OZAKI1` / `OZAKI2` はこのノブを取らない —— launcher が 1 本のデバイス作業領域を
  共有し内部で `cudaStreamSynchronize` するため**stream 再入不可**（性能ではなく
  実装の性質）。したがって `GEMM` 対 `OZAKI` の一軸比較は直列同士で読むこと。
- [x] ~~**p≥511 に今回の 2 つの知見を当てる。**~~ **どちらも「当てる先が無い」で
  閉じた**（§9.2 / §9.3）。
  (b) **経路間差は存在しなかった**: `Nq≥512` の x は単発ではなく batched GEMM で、
  `GEMM_FUSED` の `run_gemm_batched_nn_scaled` も `GEMM_CUTE` の
  `run_gemm_batched_nn` も**どちらも `RepadEpilogue<…,8>` を張った capped 版**
  （`p767_gap_study.md` §14.1 が 2026-09-01 に −0.075% で導入済み）。mainloop も
  `GemmY` と `GemmYScaleShallow` が epilogue output op だけの違い。
  **`AGENTS.md` の契約違反は無い。** ただしコメント頼みにしないため
  `run_volume_gemm_x_scale` に `static_assert` を置き、**Nq≥512 の mainloop 共有も
  コンパイル時に機械検査される**ようにした。
  (a) **N 述語化は Nq≥128 で 0%**（汎用 64×128 の N が Nq 以上になる）、
  Nq≥512 はそもそも単発 64×128 を使っていない。数え上げで閉じた。
  なお batched x 対単発 x の再点検は `p767_gap_study.md` §14.2 が済ませている
  （p=767 は差なし、p=575 は −2.94%）。
- [x] ~~**Ozaki 系列。**~~ **7 次数を現行ツリーで測り直した**（§9.5）。
  native `GEMM` 比は `OZAKI2` 3.19×（p=7）→ **1.74×**（p=511）、
  `OZAKI1` 75.4× → 13.1×、`CublasEmulation` 298× → **0.944×**。
  - **`CublasEmulation` が p=511 で初めて native を下回った**（−5.6%）。
    `p511_gap_study.md` §9 の 1.50× と逆向きで、p=255 も 8.8× → 2.02× に縮む。
  - [x] ~~**未解明 1**: 同じ実行ファイルでログイン GPU では p=511 emulation が
    1.077×（遅い側）に出る。~~ **2026-09-03 に閉じた（§9.7）。原因はノードでは
    なく `SCALE_DG_GVOLPAR`**（§9.5 の表は 0＝直列、login の裏取りは既定 2＝
    3 本並列）。**同一ノード内で両方の符号が再現する**（job `78530`、node `c187`）。
    クロック・電力・温度・他プロセス・MPS・ドライバはいずれも原因ではない
    （両環境とも `clocks.max.sm` 2062 MHz に張り付き、emulation の方が低消費電力）。
    機構は **cuBLAS emulation がハンドル 1 本と 8 GiB workspace 1 つを共有する**
    ことで、2 本目が重なった瞬間に全額 +13.8% が出る（gp1 と gp2 が同値）。
    **副産物: 既定 schedule × `CublasEmulation` は owned `dqdt` に ±Inf を
    出していた**（p=7 で 16/4096 点、3 回再現）。Ozaki backend と同じく
    `par = 0` に落とす修正を入れ、修正後は 3 通りとも対 `SPLIT` 8.88e-15、
    p=511 emulation は 12 592–12 610 µs/stage＝native の **0.948×**。
  - [x] ~~**未解明 2**: `p511_gap_study.md` §8 は `OZAKI1` 1.14× / `OZAKI2` 3.1× と
    記録しているが、本測定は 13.1× / 1.74× で**順位が逆**。~~
    **2026-09-03 に閉じた（§9.8）。原因は実装変更でもパラメータでもなく
    `EmulationMantissaControl` の既定が `fd091fc`（2026-08-29）で DYNAMIC →
    FIXED に変わったこと**で、同じ制御が Ozaki-I の残差 early-exit と
    Ozaki-II の追加 A パックを決めている。§8 は DYNAMIC、§9.5 は FIXED。
    現行ツリーで ADP に戻すと **`OZAKI1` 1.140× / `OZAKI2` 3.114×** と §8 を
    2 桁とも再現する（＝Ozaki の演算は §8 以降変わっていない）。
    仕事量でも裏が取れる: `SCALE_DG_OZAKI1_SLICE_STATS` の INT8 GEMM 代理は
    ADP 15 / FIXED 576 per step。**§8 の commit 表記 `38952e4` も誤りで、
    正しくは `dd3c4a7`**（p≥511 の Ozaki ゲートを開け、§8 本文を足した commit）。
  - [x] ~~`OZAKI2` は既定 7 moduli では owned `dqdt` が p=7 3e-01 / p=31 1e+01 と合わず、
    上の速度は「その精度での速度」である。精度を揃えた 8/14 では p=511 で 2.37×。~~
    **2026-09-03 に訂正（§9.8.5）。moduli 数は原因ではない**: `OZAKI2` は
    FIXED / ADP × 7 / 14 moduli の 4 通りすべてで p=7 が 1.9e-01〜3.0e-01 で合わない。
    効いていないのは A 側の int8 表現である。**DG の要求精度を満たす Ozaki は
    `OZAKI1` FIXED だけ**（p=7 で 3.55e-15）で、その p=511 は native の **16.6×**。
    **さらに ADP の `OZAKI1` は合わない**（3.04e-01、実効 1.33 スライス）ので、
    `p511_gap_study.md` §8 の 1.14× は無効な精度設定の速度である。
- [x] ~~**p≥511 の GEMM 系「探索終了」宣言の再点検。**~~ **`GEMM_FUSED` の宣言は
  維持、`GEMM` 側は終了していなかった**（§9.6）。
  2026-09-02 の担い手・割り当て作業は `dg_gemm_defaults` の
  `Nq = 8/16/32/64/128/256` の case にしか触れておらず、`Nq > 256` は
  `case default`（全ノブ 0）＝作業前と同一の起動列なので、
  **p≥511 の `GEMM_FUSED` / `GEMM_CUTE` は 1 バイトも変わっていない**。
  新しい軸（repad / N 述語化 / 担い手 / epilogue パッケージ）はいずれも
  当てる先が無いか既に当たっている。**残っていたのは未融合ドライバの schedule で、
  それが §9.4 の −0.222%（p=511、12/12）である** —— §6 が数えている
  「覆った探索終了宣言」と同じ形の 8 例目。
  現在地（login 3-run、µs/stage）: `GEMM` 13 299.9 / `GEMM_CUTE` 13 410.9 /
  **`GEMM_FUSED` 12 437.3（最速）**。`GEMM_CUTE` が `GEMM` に 0.83% 負けるのは
  `p767_gap_study.md` §13.2 / §14.3 の 0.45〜0.60% と同じ向きで、
  §9.4 が `GEMM` 側にだけ入ったぶん差が広がっている。

## 8. 完了（参照用）

- `reports/gemm_assignment_and_carrier.md` §6 の全項目。担い手 3 候補 × 全次数、
  volume GEMM ごとのライブラリ割り当て、epilogue パッケージ 3 成分、
  および派生した未解決 6 件。
- `GEMM_FUSED` の到達点: p=7 1747.1 → 1482.0、p=15 658.0 → 597.9、
  p=31 708.0 → 545.4、p=63 571.1 → 564.3。

## 9. 棚卸しの残り（2026-09-03、`reports/measurement_inventory.md`）

`232dd27` 時点で `reports/` の公表値を全数点検した結果、再測定ジョブ `78678`
（`jobs/inventory_sweep.sh`、GEMM 系 5 経路 × 全次数 ＋ p=511 の TC/DFMA）で
片付く分を除いて残ったもの。

- [x] ~~**p=575 / 767 の `OZAKI1` / `OZAKI2` を `3d4045c` 後に測り直す。**~~
  **2026-09-03 完了**（job `78846` PART B2、node `c186`、3 ラウンド交互、
  `measurement_inventory.md` §10）。初出の 4 セル:
  p=575 `OZAKI1` **250 147.4** / `OZAKI2` **33 497.0**、
  p=767 `OZAKI1` **618 781.8** / `OZAKI2` **82 149.1** µs/stage
  （既定 FIXED 55 bit / 7 slices / 7 moduli）。native 比は 12.1× / 1.62× /
  9.78× / 1.30×。**`3d4045c` は Ozaki を動かしていなかった** —— §9.5
  （`GVOLPAR=0`、login）と 14 セルすべてが 0.25% 以内で一致し、
  ノブの利得は INT8 スライス対の仕事量に埋もれて観測できない（§10.4）。
  副産物: **`p767_gap_study.md` §8 の「Scheme I が native を上回る最初の次数
  （0.85×）」は DYNAMIC 既定＝数値不合格の設定での速度**で、FIXED では 9.78×。
  両 §8 に訂正注記を入れた（表は書き換えていない）。
- [x] ~~**主比 A・主比 B を 6 次数 1 ジョブで揃え直す。**~~
  **2026-09-03 完了、しかも 7 次数で揃った**（job `78846` PART A、node `c186`、
  占有 GPU、**12 ラウンド交互**、物差し `CUDA device fused tendency`
  ÷ (3 × Measured steps)）。**論文表は
  [`measurement_inventory.md`](reports/measurement_inventory.md) §9.2** に置いた
  （`dfma_register_budget.md` §2 と `README.md` から前方参照を張ってある）。
  `FUSED_DFMA` は §8 の決定どおり **iso 形と単体最適形の 2 列**。単体最適形だけは
  別実行ファイル（`__launch_bounds__` がコンパイル時定数のため「同一実行ファイル」は
  原理的に満たせない。同一ジョブ・同一ラウンド列は満たす）。
  **A_iso** 1.313 / 1.641 / 2.214 / 2.052 / 2.459 / 2.271 / **2.313（p=511）**、
  **A_opt** 1.264 / 1.642 / 2.214 / 2.104 / 2.349 / 2.050 / **2.117（p=511）**、
  **B** 1.149 / 1.225 / 1.420 / 1.136 / 1.390 / 1.630（p=511 は `FUSED` の
  実装が無いので**存在しない**）。**13 個の比がすべて過去値と 0.4% 以内で一致**し、
  結論は 1 つも動かない。残る未測定は **p=511 の `MINB255_DFMA` 掃引**
  （A_opt(511) は 1 点のみ＝単体最適の下限）。
- [x] ~~**p=511 の機構比 A を成立させる。**~~ **成立した**
  （job `78678`、node `c186`、12 ラウンド交互、レンジ非重複）:
  `FUSED_TC` 13 100.8 対 `FUSED_DFMA` 30 330.7 µs/stage ＝ **A = 2.315×**。
  `p511_gap_study.md` §15.5 の login 3-run（13 124.6 / 30 338.3 → 2.312×）と
  0.13% 差で、**§25 の 6 次数表の隣に置ける 7 次数目**になった。
  同ジョブの `FUSED_TC` は `GEMM_FUSED` に +5.49% / `GEMM` に −1.38% /
  `GEMM_CUTE` に −2.20% で、§15.5（node `c182`）を別ノードで全部再現している。
  `reports/measurement_inventory.md` §7.3。
- [x] ~~**`README.md` 最速経路表の p=1023 = 187 617 µs/stage の物差し。**~~
  **2026-09-03、補正則の側は閉じた**（job `78846` PART D、
  `measurement_inventory.md` §12）。`WarmupStep=3` 固定で `nstep` を
  6/8/10/13/16（9/15/21/30/39 stage）と振って回帰すると
  **傾き 180 035.9 µs/stage・切片 +0.9999 stage**、÷(3·steps+1) の 5 点は
  180 033.6〜180 036.2（ばらつき 0.0015%）。
  **`p767_gap_study.md` §15 の +1 stage 補正は、計時 step が 3 しかない点まで
  含めて p=1023 でも厳密に成り立つ。** したがって
  **「÷22 補正が過補正」という §7.8 の読みは誤りで、§12.3 で訂正した**
  （÷22 の 180 357 は真値 180 036 の +0.18%）。合わないのは
  `p1023_gap_study.md` §13.2 の nsys trace **186 060** の側で、これは同一実行の
  `Step loop per stage`（1 step 全体）184 872 より大きく、テンダンシーだけの
  device 時間ではありえない。
  **2026-09-03 夕、nsys を当て直して 186 060 の正体を特定した**
  （job `79016`、node `c390`、tree `9ad5fb0`、conf 無変更、
  `measurement_inventory.md` §12.5 と `p1023_gap_study.md` §14）。
  **186 060 は stage 周期であり、定義上 RK 更新と halo を含む。**
  カーネル単位の内訳: テンダンシー小計 **180 226.1**（同一実行の
  `CUDA device GEMM fused` ÷22 = 180 245.5 と **0.011%** 一致）、
  ＋ `rk_update` 4 759.4 ＋ halo 79.8 = **185 065.4**（同一実行の
  `Step loop per stage` 185 079 と **0.008%** 一致）、＋隙間 82.2 =
  stage 周期中央値 **185 147.6**。**profiler のオーバーヘッドは検出できない**
  （nsys 下のほうが 0.09% 速い）。つまり §12.4 の候補 (a) が正解で、
  誤っていたのは §13.2 の表ではなく突き合わせ相手のほうだった。
  残る 186 060 対 185 148 の **0.49% は帰属できていない**（29 周期の平均を
  先頭区間込みで取ると 186 269.9 で公表値と 0.11% 一致するが、§13.2 が
  どの統計を使ったかは本文に無く、当時の trace も実行ファイルも残っていない）。
  **どのジョブの測定が 187 617 かも依然として特定できていない**（÷21 系という
  系統だけが §7.8 で確定済み）。以下は当時の記録:

  > **旧記載（2026-09-03 の午前、job `78678` 時点）**: 系統は確定した
  > （÷21 = 188 945.5 / ÷22 補正 = 180 357.0 / `Step loop per stage` = 185 195.5）。
  > 表の 187 617 は ÷21 系（0.7% 差）。どのジョブの測定かは特定できていない。
  > さらに新しい未解明が 1 つ出た: ÷22 補正（180 357）は nsys trace の 186 060 より
  > 3.1% 小さく、p=1023 では §15 の +1 stage 補正が過補正になっている。
  > `reports/measurement_inventory.md` §7.8。
  > —— **この最後の 1 文が誤りだったことを §12 が示した。**
- [x] ~~**`p767_gap_study.md` §13 / §14 の `Cal_tend` 系の値に注記を足す。**~~
  **2026-09-03 完了。** §13 と §14 の見出し直下に「物差しの注記」ブロックを
  置き、(a) 値が `Cal_tend` ÷ 75 stage / ÷ 33 stage であること、
  (b) 偏りが −1.3% / −3.0% であること、(c) **横断比較に使ってはならない**こと、
  (d) 正しい絶対値は §15.6 のテンダンシー **58.592 ms/stage**、
  (e) **同一 conf・同一 stage 数の A/B 判定は 1 つも動かない**こと、
  (f) 現行値は `measurement_inventory.md` §7.4（60 294.4 µs/stage）であること、
  を §15 への内部リンク付きで書いた。§13 と §14 の絶対値が 1.77% 違うのが
  性能差ではなくこの偏りの差であること（§15.1）も明記した。表は書き換えていない。
- [x] ~~**`WarmupStep` 導入（`760e96e`、2026-08-27）より前の値の特定。**~~
  **2026-09-03 完了（測定不要、git 考古学で確定）。`measurement_inventory.md` §8。**
  `760e96e` は **2026-08-27 17:59:49**、既定は `WarmupStep = 1` なので、
  **このコミット以降のツリーで作った実行ファイルは conf に何も書かなくても
  先頭 1 step が計時外**である。判定:
  - `p63_gap_study.md` §13 —— 節を追加したのは `d6c935e`（**08-27 23:20:10**、
    `760e96e` は祖先）。**warmup 除外済み。**
  - `p15_gap_study.md` §17 / `p31_gap_study.md` §15 —— 2026-08-28 追記で、
    **本文が `WarmupStep=1` を明記している**。**warmup 除外済み。**
    （`measurement_inventory.md` §2.3 の「明記していない」は誤りで、§8 で訂正した。）
  - `overall_summary_report.md` —— **混在**。`508c0d0`（08-30）が
    `execution_times.md`（08-30 まで更新＝**除外済み**）と
    `gpu_optimization_session_report.md`（最終更新 `c66bfca` **08-27 12:55**＝
    `760e96e` の 5 時間前＝**warmup 込み**）を畳んだもので、
    **段落ごとに折り込み元を辿れば確定する**（`git show 508c0d0 --` で 1 行ずつ追える）。
  偏りの大きさは `760e96e` 自身が「p=7 で per-step +1.5% 程度、run-to-run の
  ばらつきの大半」と書いている。**「確定できない」ではなく「確定した」で閉じる。**

### 9.1 棚卸しの再測定（job `78678`）で新たに立った項目

- [x] ~~**`OZAKI1` / `OZAKI2` の per-stage 絶対値がどこにも無い。**~~
  **2026-09-03 完了。ただし本項目の前提は 2 つとも誤りだった**
  （`measurement_inventory.md` §10）。
  1. **§9.5 は絶対値を公表していた。** 表のセルは `123 111（75.4×）` の形で、
     **括弧の外が µs/stage** である。本項目と §7.10 の「絶対値が無い」は読み違い
     （§10.0 で訂正）。
  2. **12 ラウンドに収まらない、も誤り。** 実測では p=7 `OZAKI1` が 1 run 10.4 秒で、
     9 次数 × 2 経路 × 6（p≥575 は 3）ラウンドが 25 分で終わった。
  測り直した値（job `78846` PART B1/B2、node `c186`、占有 GPU、
  **既定 FIXED 55 bit / 7 slices / 7 moduli**、µs/stage、p=7…767）:
  `OZAKI1` = 123 257.7 / 117 916.3 / 41 918.6 / 28 598.5 / 24 884.2 / 24 596.6 /
  174 695.8 / **250 147.4** / **618 781.8**、
  `OZAKI2` = 5 212.8 / 5 086.5 / 3 479.6 / 3 321.1 / 3 332.0 / 3 190.9 /
  23 188.1 / **33 497.0** / **82 149.1**。p=1023 は OOM で**存在しないセル**。
  **精度の明記（必須）**: `OZAKI1` FIXED は数値が合う（3.55e-15）ので
  この列は DG に使える設定の速度、**`OZAKI2` は FIXED/ADP × 7/14 moduli の
  4 通りすべて不合格**（1.9e-01〜3.0e-01）なのでこの列は使えない設定の速度である
  （`gemm_assignment_and_carrier.md` §9.8.5）。
  §9.5（login、`GVOLPAR=0`）とは 14 セルすべて 0.25% 以内で一致した。
- [x] ~~**p=7 で `GEMM_CUTE`（1479.8）が `GEMM_FUSED`（1483.3）より 0.24% 速い。**~~
  **機構を同定した**（`gemm_assignment_and_carrier.md` §10.8。job `78850` c394
  12 ラウンド交互で 1478.2 対 1481.9 を再現、ncu job `78852`、nsys job `78851`）。
  **棚卸しが立てた「受け皿が y しかない」という筋は誤りだった**: Nq=8 の担い手は
  すでに **x**（`carrier=2`）で、`Escale` 前送りは効いていない。真の分解は
  `separable_lift_assembly` が消える **−169.1 µs/stage** に対し、
  同一タイル・同一 grid の x GEMM が融合 epilogue で **97.1 → 279.2 µs/stage
  （+182.1）** になり、正味 **+13.0 µs（+0.88%）**。x 担い手だけが使える y‖z が
  **−8.1 µs** 買い戻して実測 +3.7 µs（予測 +4.9、1.2 µs で閉じる）。
  epilogue の代金はレジスタで、**64 → 126 reg/thread → 理論占有率 43.8 → 25.0%、
  waves/SM 30.80 → 53.89、Issued IPC 3.170 → 2.370、SM 76.8 → 58.4%**。
  Nq=8 は mainloop が K=8 と短く CTA が 54 波あるので、
  **占有率半減の代金を波の数だけ払う**のが融合の得を食い切る。
  融合が得になる境界は **Nq=8 と Nq=16 のあいだ**（p=15 は融合が 8.5% 得）。
  副産物: **`GEMM_FUSED` は p=7 で flux 融合（`flux_all`）も手放している**ので、
  「融合はカーネルを 1 本減らす」は p=7 では成り立たない（launch 本数は 8 で同じ）。
- [x] ~~**`GEMM_CUTE` ÷ `GEMM` の符号が Nq=64 で反転する機構。**~~
  **機構を同定した**（`gemm_assignment_and_carrier.md` §10.1–§10.6）。3 点ある。
  (1) **§7.6 の 10 点は一軸ではなかった** —— `SCALE_DG_GVOLPAR`（既定 2）は
  未融合ドライバしか読まないので `GEMM` だけが 3 本の volume GEMM を別ストリームへ
  分散し、`GEMM_CUTE` は直列。スケジュール軸が 1.2〜2.8% 混ざっている（下の新項目）。
  (2) **一軸（`GVOLPAR=0`）に揃えた比**は job `78850`（c394、12 ラウンド交互、
  レンジ非重複）で **0.9048 / 0.7755 / 0.9740 / 1.0151 / 1.0099 / 0.9867**
  （Nq=8…256）＋ **1.0049**（p=512、login 3-run）。**反転は残り位置も動かないが、
  負け幅の最大は Nq=64 の +1.51% に縮み、Nq=256 では符号が戻って CUTLASS が
  1.33% 勝つ。** Nq=576 / 768 / 1024 は測れていない
  （→ **2026-09-03、job `78993` で埋まった: 0.9978 / 1.0044 / 1.0019。
  `gemm_assignment_and_carrier.md` §11.1**）。
  (3) **符号を決めているのは z 1 本だけ。** 同一ドライバ上で x/y/z のライブラリを
  1 本ずつ振ると効果は 6 次数すべてで**加法的**（残差 1 µs 前後）で、
  Δz は `Nq>=64` の 4 次数すべてで負（−12.4 / −4.9 / −5.2 / −65.0 µs、
  p=63/127/255/511）。**`Nq<=32` の勝ちはタイル適合**（cuBLAS が Nq より広い
  タイルを選び `1−Nq/TbN` を述語で捨てる。p=7 の nsys は x −87.8 / z −67.3 /
  y −3.1 µs でステージ差 −155.5 を 2.7 µs で閉じる）で、**Nq=16 の 0.788 は
  y 単独が +114.1 µs＝ステージの 17.4%** を占めることによる。
  **`Nq>=64` の負けは mainloop の弱さではない**（x/y は cuBLAS と同じ grid・同じ
  waves で SM throughput はむしろ CUTLASS が高い）。負けているのは
  **CUTLASS の z が 64 スレッド（2 ワープ）CTA を 8192 個並べる**ことで、
  waves/SM が 13.47（cuBLAS 6.74〜8.98）、最終波が 47% しか埋まらず、
  実占有率 12.1% / SM 64.0% と 3 本で最も低い。
  (4) **「固定費の tail 償却」という説明は反証された**: conf は次数とともに `Ne` を
  落とすので **grid と waves/SM は p=63 と p=127 で完全に同一**であり、tail 損は
  次数の関数ではない。減衰しているのは K=Nq が伸びて **CTA 固定費の相対比重が
  下がる**ほう。
  (5) **2026-09-03 夕、prologue と tail の内訳を測った**
  （job `79016` node `c390` / `79017` node `c395`、tree `9ad5fb0`、
  `gemm_assignment_and_carrier.md` §10.10）。
  **tail（最終波の充填不足）= z カーネルの 3.05〜3.73%、次数によらず一定**
  （grid は p=63/127/255 とも 8192 CTA、`ncu` の waves/SM も 3 次数とも 13.47）。
  **grid の launch/drain = 1.55%（p=63）。** 波の量子 **Q = 608 CTA**
  （＝152 SM × 4 CTA/SM）は、`Ne` を 4〜76 と振った**機構同定専用**の掃引が
  階段の段差位置から独立に出し、`ncu` の 13.47 と一致した。
  29 点は **T = 1.522 + 6.976·⌈CTA/608⌉ µs**（R²=0.99845、rms 0.994 µs）に乗り、
  端数を許す線より当てはまる＝波量子化の直接証拠。
  **prologue は命令数としては正確に測れた**: 多段を 1 段減らすと grid 全体で
  **1 589 248 命令**減り、これが **p=63 と p=255 で 1 命令も違わない**
  （＝CTA あたり 194 命令 ＝ 97 ワープ命令 ＝ A 64×16 と B 16×32 の
  global→shared 1 段ぶんの理論値 96 と一致）。3 段の充填で
  **p=63 は全命令の 13.2%、p=255 は 5.1%**。
  **未解決: prologue の「時間」ぶんは分離できない。** 段数は共有メモリ量
  （1 段 12 288 B）を決め、それが Block Limit Shared Mem（S4 で 4、S3 で 6）を
  決め、waves/SM（13.47 対 8.98）を決めるので、**`Stages` を動かすと
  prologue と tail が必ず同時に動く**。共有総量を保ったまま段数だけ変える
  実装が要るが、していない。
  副産物（**採否ではない**）: `Stages=3` は p=63 で −1.42%、p=255 で −0.20%
  （8 ラウンド交互、レンジ非重複、`dqdt` 全点ビット一致）。
  `GEMM_CUTE` は単独最適化しない対照であり `GEMM_FUSED` と mainloop を
  揃える義務があるので、**共有 mainloop の変更として別途判断が要る**（下記）。
- [x] **`GEMM_CUTE` / `GEMM_FUSED` 共有の平 z を `Stages=3` にするか（新規、2026-09-03。**閉じた: 2026-09-03、job `79164`**）。**
  `gemm_assignment_and_carrier.md` §10.10.5 / §10.10.8。既定の 64x32 / S4 は
  共有 48 KB で SM あたり 4 CTA しか置けず waves/SM 13.47、最終波 47%。
  S3 なら共有 36 KB / 6 CTA / waves 8.98 で tail がほぼ消える。
  wall は p=63 −1.42% / p=127 −0.23% / p=255 −0.20%（job `79017` PART D、
  8 ラウンド交互、レンジ非重複、数値はビット一致）。
  **採否の条件**: (a) `launch_volume_gemm_z` は両経路が通る共有ヘッダなので
  既定を動かせば両方に同時に入る（役割上それが正しい）、(b) ただし
  `GEMM_FUSED` の**担い手 z**（`run_z_gemm_assembly` は集合自身の `GemmZ` を
  使う）は別物なので、そちらも同時に測る必要がある、(c) p=7 / 15 / 31 は
  z が cuBLAS だったり別タイルだったりするので次数横断で測り直すこと。
  **本作業では既定を動かしていない**（ノブ `SCALE_DG_ZTILE` の `Nq>=32` 拡張は
  既定 `-1` ＝ production dispatch 外）。

  **閉じた（2026-09-03、job `79164`、node `c183`、`gemm_assignment_and_carrier.md`
  §12.2）。** 条件 (a)(b)(c) をすべて満たす形で測った: `ZStage3Set` が集合の
  `GemmZ` と `GemmZWide` をまとめて 3 段に差し替えるので、**平 z（`GEMM_CUTE`）と
  担い手 z（`GEMM_FUSED`）が必ず一緒に動く**。7 次数 × 2 経路の 12/6/4 ラウンド
  交互で、**Nq = 64 / 128 / 256 は両経路とも勝ち**（`CUTE` −1.40 / −0.23 /
  −0.25%、`FUSED` −0.54 / −0.66 / −0.31%、いずれもレンジ非重複）、
  **Nq >= 512 は `FUSED` が負ける**（Nq=512 +0.09%、Nq=1024 +0.05%、
  Nq=576 / 768 はレンジ重複＝差なし。`CUTE` は −0.09〜−0.13%）。
  **採用は Nq = 64 / 128 / 256 に限り、`z_stage3_default()` として両経路に
  同時に入れた**（override は `SCALE_DG_ZS3`）。全次数で `dqdt` はビット一致。
  p=63 の −1.40% は §10.10.5 の `ZG1`(S3) −1.42% の独立再現（別ジョブ・
  別ノード）。**Nq = 8 / 16 の z はもともと 3 段、Nq = 32 の z は cuBLAS**
  なので条件 (c) は「そこに 4 段の z が無い」で閉じる。
  **残る未測定**: Nq >= 512 で `FUSED` の符号が変わる機構（この 2 次数の
  `ncu` は取っていない）、`GemmZScale`（x 担い手の z、既定では到達しない）の段数。
- [x] **`xy_weighted` を `Nq>=128` で個別に測ったかどうかが確認できていない（新規、2026-09-03。**閉じた: 2026-09-03、job `79164`**）。**
  `mod_cuda_dg_kernels.cuf:2861` の条件は `fuse_epilogue .and. Nq >= 64` の
  一括で、実測の記録があるのは Nq=64（`p63_gap_study.md` §25、x‖y の 595.2 に対し
  畳み込み 570.9 µs/stage ＝ −4.085%）と、棄却された Nq=32（+2.3%）だけである。
  **Nq=128 / 256 / 512 で畳み込みが実際に勝つかは確認していない。**
  この条件は `overlap_y` の `Nq<64` 条件と表裏なので、
  もし高次数で畳み込みが負けているなら、そこでは重ねを使えるはずであり、
  `gemm_assignment_and_carrier.md` §11.6 の「Nq>=64 の融合軸は下限」も変わる。

  **閉じた（2026-09-03、job `79164`、node `c183`、`gemm_assignment_and_carrier.md`
  §12.1）。畳み込みは Nq = 128 / 256 / 512 / 576 / 768 / 1024 の 6 次数すべてで
  勝つ**: 切ると **+48.10 / +33.53 / +16.39 / +13.93 / +10.51 / +5.90%**、
  全次数レンジ非重複。切ったうえで非担い手 2 本を重ねても取り戻せるのは
  **Nq=128 で 0.92%、Nq=256 で 0.89%、Nq >= 512 ではゼロ（レンジ重複）**。
  したがって条件は 7 次数ぶん実測で裏づけられ、Nq=32 の +2.3% と合わせて
  **未測定の次数はもう無い**。**§11.6 の「Nq>=64 の融合軸は下限」は変わらず
  維持され、さらに下端で閉じる**: `GEMM_FUSED` が重ねを持てる唯一の道は
  畳み込みを捨てることで、その取引は 6〜50 倍の赤字なので、
  **`GEMM_FUSED` は Nq>=128 ですでに自分に許された最良形**であり、
  §11.6 の「CUTE 重ね」列がそのまま like-for-like の値になる。
  得は Nq とともに減衰し、中央 3 区間は 1/Nq とよく合う。
  ノブ `SCALE_DG_XYW` / `SCALE_DG_XYOVL` は**役割外アブレーション、既定オフ、
  production dispatch 外**のまま残した。
  **残る未測定**: Nq>=128 の担い手が 5 本読みでレジスタをこぼしているか
  （`ncu` を当てていない）、減衰の両端 2 区間が 1/Nq から外れる理由。

- [ ] **`GEMM` 対 `GEMM_CUTE` の一軸性が壊れている（新規、2026-09-03）。**
  `SCALE_DG_GVOLPAR`（既定 2、§9.4 で採用）は `cuda_cal_dqdt_gemm_unfused` しか
  読まないので、**`GEMM` は 3 本の volume GEMM を別ストリームに分散し、
  `GEMM_CUTE` は `dg_stream` 上で直列に走らせる。** `AGENTS.md` は
  「surrounding kernels を共有することが `GEMM` / `GEMM_CUTE` / `GEMM_FUSED` の
  比較を一軸にしている」と書いているが、**volume GEMM の並べ方は共有 launch
  ヘルパを通らないのでその規則をすり抜けている。** 実害: 既定比と一軸比で
  **符号が変わる次数がある**（Nq=32 は 0.991 → 0.974、Nq=256 は 1.009 → 0.987）。
  **これは比較設計の defect であって経路役割の defect ではない**
  （`GEMM_CUTE` は `GEMM_FUSED` と揃っていればよく、`GEMM` と揃う義務はない。
  `GVOLPAR` の採用自体も「baseline を強く保て」という規定に沿っている）。
  是正案 4 つと、それぞれで言えなくなることは
  `gemm_assignment_and_carrier.md` §10.7 の表。**(A) CUTLASS ドライバにも同じ
  分散を入れるとエピローグ融合軸のほうが汚れる**ので、
  **`AGENTS.md` が「2 つの比のどちらを一軸にしたいのか」を先に決める必要がある。**
  筆者の推奨は **(D) per-GEMM 掃引を正式な軸にする**（加法性を 6 次数で確認済み）
  を主、**(C) 比較時は `GVOLPAR=0` で揃えると規約に書く**を従。
  **実装・既定変更は行っていない**（`3d4045c` / §9.4 は A/B を取って採用したもの
  なので、覆すには別途の判断が要る）。

  **追記（2026-09-03、job `78993` / `78999`）: 混入量が 10 次数すべてで測れ、
  §10.7 の案 A の評価が誤りだったことが分かった。判断そのものは未決のまま。**
  - **一軸（`GVOLPAR=0`）の 10 点が揃った**（node `c089`、占有 GPU、
    p≤511 は 12 ラウンド・p≥575 は 6 ラウンド交互、10 次数すべてレンジ非重複、
    `gemm_assignment_and_carrier.md` §11.1）:
    **0.9050 / 0.7733 / 0.9748 / 1.0149 / 1.0107 / 0.9873 / 1.0050 / 0.9978 /
    1.0044 / 1.0019**（Nq=8…1024）。§10.2 の 7 点を別ノードで
    最大 0.0022 ポイント差で再現し、未測定だった Nq=576 / 768 / 1024 が埋まった。
  - **混入量の次数依存**（§11.2）: **0.0107 / 0.0147 / 0.0159 / 0.0165 / 0.0284 /
    0.0212 / 0.0032 / 0.0019 / 0.0007 / −0.0001 ポイント**。
    **Nq=128 が最大**で、両端に向かって落ちる。**低次数側は測れた。**
  - **§10.7 の案 A の評価は逆だった**（§11.5–§11.7 で訂正注記）。融合は
    重ね合わせの機会を **3→2 に減らす**（未融合は 3 本独立、融合は担い手が
    他の 2 本に依存。担い手 launch は 3 変種とも `deriv_x, deriv_y` を取る）。
    **案 A は両方の軸を同時に改善しうる。**
  - **依然として未決**: どの案を採るか。**実装・既定変更はしていない。**
- [x] ~~**p=767 / 1023 の `GEMM` が当時値より +0.9% / +1.5% 遅い件。**~~
  **2026-09-03 完了。回帰ではない。測定条件差である**（job `78994`、node `c092`、
  占有 GPU、6 ラウンド交互、`measurement_inventory.md` §13）。
  **当時の実行ファイルは無くてもソースは git にある**ので、当時ツリーを
  worktree でビルドして新旧 A/B を取った（p=767 は当時値を記録した
  `9402f72` そのもの、p=1023 は `4c2b65a` が linearized 履歴でビルド不能な
  ため最小修正の `38952e4`。native `GEMM` 経路は同一）。
  - **当時 conf と現行 conf が違う**: p=767 は `nstep=30`/`WarmupStep=5`（75 stage）
    → `nstep=15`/`WarmupStep=4`（33 stage）、p=1023 は `nstep=15`/`WarmupStep=3`
    （36 stage）→ `nstep=10`/`WarmupStep=3`（21 stage）。÷(3·steps) の +1 stage
    偏りだけで **+1.675% / +1.931%** 上がる計算。
  - **conf 軸の実測は +1.69% / +1.93%**（旧新 2 本の実行ファイルで 4 例、
    予測と 0.02 ポイント以内）。**バイナリ軸は p=767 −0.60%（レンジ非重複、
    新のほうが速い）、p=1023 はレンジ重複＝差が無い。**
  - 旧バイナリ＋当時 conf は **62 742.9 / 193 329.9** で、公表値 62 817 / 194 058 を
    **−0.12% / −0.38%** で再現（残差は login 3-run 対 占有ノードの差）。
  - **回帰が無いので二分探索する対象も無い。** 以下は当時の記録:

  > **旧記載（2026-09-03 午前、job `78846` 時点）**: **p=767 / 1023 の `GEMM` が
  > 当時値より +0.9% / +1.5% 遅い件。**
  > **2026-09-03、原因の候補から `SCALE_DG_GVOLPAR` を外した**
  > （job `78846` PART C、**同一バイナリ** `scale-dg_extraction.inv232dd27`、
  > 同一 conf、10 ラウンド交互、node `c186`。`measurement_inventory.md` §11）。
  > - **「利得はこの次数で実質ゼロ」を数値にした**:
  >   p=767 は `=2` **63 292.3** 対 `=0` 63 334.5 = **−0.067%**（レンジ非重複）、
  >   p=1023 は `=2` 196 702.4 対 `=0` 196 693.3 = **+0.005%（レンジ重複＝差が無い）**。
  > - **直列（当時と同じ schedule）に戻しても当時値には戻らない**
  >   （63 334.5 は 62 817 の +0.82%、196 693.3 は 194 058 の +1.36%）。
  >   **したがって差の原因は `3d4045c` の schedule 変更ではない。**
  > - **回帰かどうかは依然として断定できない。** 2026-08-29 当時の実行ファイルが
  >   凍結されておらず（`ls scale-dg_extraction.*` に 08-29 以前の凍結物は無い）、
  >   **同一入力での新旧バイナリ A/B が原理的に取れない**ためである。
  >   残る候補は (a) login 3-run 対 占有ノード交互の測定条件差、
  >   (b) `3d4045c` 以外のコード変更の 2 つで、**本作業では分離できない。**
  > - **併せて分かったこと**: `SCALE_DG_GVOLPAR` は未融合ドライバしか読まないので、
  >   既定では **`GEMM` だけが 3 本の volume GEMM を別ストリームに散らし、
  >   `GEMM_CUTE` は直列**である。したがって §7.6 の `GEMM_CUTE` ÷ `GEMM` の
  >   10 点には **schedule 軸が混入していて一軸ではない**。混入量が測れたのは
  >   右端 2 点だけで、**p=767 で 0.067 ポイント（0.5 ポイント中の約 13%）、
  >   p=1023 でゼロ**。**低次数側（CUTE が 0.79〜0.92 で勝つ領域）の混入量は
  >   測れていない**（下の「符号が Nq=64 で反転する機構」と同じ場所で測るべき）。
- [ ] **融合軸（`GEMM_FUSED` ÷ `GEMM_CUTE`）も一軸ではない（新規、2026-09-03）。**
  未融合なら 3 本の volume GEMM は独立で 3 本とも重ねられ、融合すると担い手が
  他の 2 本の微分を要素ごとに読むので重ねは 2 本まで（`launch_x_gemm_assembly` /
  `launch_y_gemm_assembly` / `launch_z_gemm_assembly` はいずれも
  `deriv_x, deriv_y` を取る）。**融合は重ね合わせの機会を 3→2 に減らす。**
  ところが現状は **`GEMM` が 3 本・`GEMM_FUSED` が 2 本（Nq≤32 のみ）・
  `GEMM_CUTE` が 0 本**で、融合が支払うはずの代金が計上されないどころか
  融合側に得が乗っている。役割外アブレーション **`SCALE_DG_CUTEPAR`**
  （既定 0、production dispatch 外、`DqdtKernel_Type` から到達しない）で
  `GEMM_CUTE` に重ねを与えて測ると（job `78999`、node `c094`、12 ラウンド交互、
  `gemm_assignment_and_carrier.md` §11.4–§11.7）、**融合軸は
  0.0074 / 0.0125 / 0.0152 / 0.0134 / 0.0200 / 0.0136 / 0.0025 ポイント
  過大**（Nq=8…512）。最大は Nq=128 の 2.00 ポイント（0.8651→0.8851）。
  **Nq=8 では結論の大きさが倍以上**（融合の負け 0.33%→1.06%）。
  **3 本目の重ねは 1 本も買わない**（par2 対 par1 は 7 次数ともレンジ重複）ので、
  汚れは「3→2」ではなく **「0→2」の側**から来ている。
  **未測定・未決**: `GEMM_FUSED` は Nq≥64 で重ねを 1 本も使っていない
  （担い手 z、`overlap_y` の `Nq<64` 条件）ので、Nq≥64 の上の 4 点は
  like-for-like ではなく**下限**である。**非担い手 2 本を Nq≥64 で重ねた
  `GEMM_FUSED` は測っていない**（`launch geometry` を動かすので経路定義に触る）。
  **実装・既定変更はしていない。利用者の判断が要る。**
- [x] ~~**過去値の Slurm job ID が取れないセル。**~~
  **2026-09-03、「取得不能」として閉じた**（`measurement_inventory.md` §14.1、
  測定不要）。**記録漏れではなかった**: p=63 `GEMM_CUTE`/`GEMM_FUSED`
  （`p63_gap_study.md` §22）、p=127 `GEMM`/`GEMM_CUTE`（`p127_gap_study.md` §15）、
  p=255 `GEMM`/`GEMM_CUTE`（`p255_gap_study.md` §12）、p=127 / p=255 の Ozaki
  （同 §14 / §11）は、いずれも節が測定条件として
  **「login node GPU 1、3-run 中央値」「GB200 login ノード」と明記**しており、
  `sbatch` を通していない。**したがって job ID は概念として存在せず、
  追う対象が無い。** commit（`2dadc41` / `38952e4`）と測定条件は記録されている。
  現行値の一次ソースは job **`78678`**（GEMM 系、`measurement_inventory.md` §7.5）
  と job **`78846`** PART B1/B2（Ozaki、同 §10.3）。
  Ozaki は当時が 8 slices / 14 moduli、現行が既定 FIXED 7/7 で
  **精度設定が違う**（§10.5）。**過去の表は書き換えていない。**
- [x] ~~**p=127 の `GEMM_FUSED` / `GEMM_CUTE` の §21 以降の絶対値。**~~
  **2026-09-03、「部分的にしか復元できない」として閉じた**
  （`measurement_inventory.md` §14.2、測定不要）。
  §21（repad）の `GEMM_FUSED` だけは絶対値が**存在する**が
  **`Cal_tend` 系**（0.21631 → 0.21525 s/99 step ＝ 728.3 → 724.7 µs/stage、
  job `74821` c185）で、公表表の `CUDA device` 系との換算係数は公表されて
  いないので**接続できない**（係数を仮定すれば数字を作ることになるのでしない）。
  **`GEMM_CUTE` の §21 段はどの物差しでも記録が無く**（§21 は CUTE の A/B を
  取っていない）、y 3 段の追記は両経路とも % のみ（`p511_gap_study.md` §12.4）。
  現行値は 641.5 / 742.2（job `78678`）で立っており**実害は無い**。
